{
  "name": "treinador-pwa",
  "private": true,
  "scripts": {
    "check": "node --check js/app.js && node --check js/db.js && node --check js/head_coach_memory.js && node --check js/head_coach_media.js && node --check js/workspace.js && node --check js/agent_contract.js",
    "test": "node --test tests/*.test.js && node js/db.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
