# Próximas fases Vision Coach — 22/09/2026

## Fase 1 — treino completo
Implementação nesta entrega: presenças e histórico individual, iniciar/pausar/retomar/terminar, cronómetro por bloco e total, notas editáveis, resumo realizado, duplicação sem copiar execução e reordenação com notas preservadas. Guia: training-session.md.
O treino planeado é preservado. Imagens e consulta anteriores não são redesenhadas.

## Fase 2 — continuidade do Head Coach
Implementado: avaliação ligada a memória única, rascunho com evidências, revisão pela IA autorizada, aprovação explícita e ligação ao treino seguinte com resultado assinalado. Guia: training-continuity.md. O rascunho inicial é um modelo local, não análise automática de toda a época; agregação de tendências fica para aprofundamento posterior.

## Fase 3 — jogo e quadro tático
Implementado o primeiro incremento: campo 5v5, alinhamento e suplentes, plano de rotações, substituições e trocas de funções confirmadas, cronómetro e minutos registados por atleta/GR. O quadro destaca também a diferença de utilização e quem ficou abaixo de metade do tempo decorrido, em ordem alfabética e sem ranking. Guia: match-visual.md.
Incremento adicional local: editar ou anular movimentos individuais na cronologia, com auditoria e recálculo de minutos; operações MCP específicas usam as mesmas validações e recusam sequências dependentes incompatíveis. Testes locais de navegador e emulação móvel incluídos; continua pendente a verificação física de sincronização entre dispositivos.
Implementado o segundo incremento: lances do jogo com minuto, atleta, lado, zona e motivo (perdas), estatísticas contadas com proveniência, posse medida/estimada/desconhecida e operações MCP. Guia: match-events.md. Implementada localmente a análise pós-jogo estruturada, ligação explícita à memória, preparação do treino seguinte e operações MCP de leitura/proposta. Guia: match-analysis.md. A preparação regista sistema, estilo, pontos fortes e vulnerabilidades observados do adversário; o campo visual guarda cones e setas táticas com edição explícita e MCP autorizado. Continuar a rever usabilidade do quadro no campo e testar sincronização em dispositivos reais.
O posicionamento visual e os lances agora partilham a mesma conversão das coordenadas do campo para três terços × três corredores. O quadro desenha as nove células, expõe a zona de cada marcador e o MCP devolve `position_zones`; a função única está em `VisionMatchEvents.zoneFromPoint`.

## Fase 4 — relatórios na app
Implementado localmente: folhas A4 imprimíveis com opção Guardar como PDF para treino (inclui imagem aprovada), ficha de jogo, relatório pós-jogo, atleta e equipa. Os relatórios de equipa e atleta usam período de época quando escolhido e incluem objetivos, avaliação semanal, prioridades e proveniência dos dados. Uma época já inexistente não alarga o relatório a todo o histórico; minutos não registados não são apresentados como zero, escalações não iniciadas não contam como titularidade, minutos registados a zero ficam distintos dos desconhecidos e objetivos associados a outras épocas identificam a evidência fora do intervalo. Estados de presença por marcar não contam como presenças. Minutos no relatório de equipa aparecem alfabeticamente, sem criar uma classificação; as posições individuais derivam dos movimentos registados e também são devolvidas na consulta MCP de utilização. A exportação A4 foi gerada em Playwright com 100 acontecimentos e validada como multipágina; a folha pós-jogo também foi verificada em 390 px sem overflow horizontal. Guias: reports.md e seasons.md. Pendente: validação numa impressora física e em telemóveis reais.

## Fase 5 — desenvolvimento dos atletas
Implementado localmente: objetivos individuais com data, evidências, exercícios relacionados, estado explícito, histórico e operações MCP; a ficha longitudinal mostra convocações, titularidades, entradas/saídas com minuto e minutos registados, sem assumir zero quando não há utilização medida. `get_player_participation_history` expõe a mesma evolução e presenças ao Head Coach em leitura remota, com isolamento de equipa e UUID estável. Testes incluem convocação sem minutos, jogo com reentrada, histórico MCP e integração ao Supabase local. Guia: player-development.md.

## Fase 6 — semana e época
Implementado localmente: painel de semana e evolução da equipa, planos semanais com referências estáveis a treinos/jogo, nomes e ligações às fichas, relação entre sessões e avaliação explícita; objetivos de equipa com fluxo identificado → planeado → trabalhado → observado → avaliado → melhorou/continua, observação/interpretação/hipótese/avaliação/decisão separados, sessões, exercícios e evidências com ligação às origens, e operações MCP. Estados avaliados exigem avaliação e decisão do treinador, sem avanço por inferência. Arquivo de épocas com plantel por UUID estável, ativação/arquivo sem apagar histórico, contagens por período e relatórios de atleta filtrados por data; operações MCP com revisão e confirmação. Guias: team-development.md e seasons.md.

## Fase 7 — vídeo
Implementado localmente: URL, timestamp, categoria, atleta opcional, descrição e ligações tipadas a observações do mesmo jogo, estatísticas e problemas com conteúdo na análise; edição/apagamento, sincronização pelo jogo e validação MCP com UUIDs estáveis. Guia: match-video-evidence.md.

Novo incremento local: `get_recurring_match_patterns` agrega perdas explicitamente registadas em jogos ativos, por motivo e zona, e devolve UUIDs e IDs de eventos de origem. Exige repetição em pelo menos dois jogos; não transforma notas ou hipóteses em factos nem recomenda prioridades. Teste MCP de leitura e contagem incluído. A consulta jogo↔treino equilibra evidências das duas fontes. `evaluate_cross_session_relation` revalida duas citações exatas e pede ao Jev uma probabilidade Noul de apoio semântico, sem escrita ou decisão automática; limita a avaliação à afirmação e às citações escolhidas. Testes usam fetch simulado, incluindo repetição limitada com `Retry-After` para respostas 429/529. Falta configurar a chave apenas no runtime Edge, validar a chamada real e publicar após revisão do treinador; sem chave, esta operação fica indisponível.

Continuação: `get_cross_session_evidence` fornece ao Head Coach leituras recentes de análises/lances de jogos e avaliações/notas de treinos, com classificação por facto registado, observação, interpretação, hipótese, avaliação ou decisão do treinador. Inclui referências UUID e sinaliza truncagem; não grava memória nem proposta. Testes MCP cobrem as categorias e a leitura sem escrita. Permanece necessária a revisão do treinador a qualquer relação semântica ou prioridade gerada pelo Head Coach.

Novo fluxo local: `prepare_cross_session_priority` valida citação, UUID, pertença à equipa e versão atual de cada fonte; guarda o objetivo como proposta do Head Coach na coleção de documentos existente e evita duplicados por identidade/idempotência. Uma nova versão da fonte gera identidade de proposta própria. A ficha mostra fontes e permite editar interpretação/hipótese. Aceitar requer decisão escrita e confirmação do treinador; rejeitar também confirma e preserva histórico. Nenhuma ação cria treino. MCP específico de preparação/aceitação/rejeição e testes de conflito incluídos.

## Transversal
Implementada localmente a pesquisa por atleta, adversário, treino, jogo/lance, exercício, objetivo, observação, documento e media, com filtros de data/época e ligação à origem. Guia: search-history.md.
Correção de UX nesta continuação: a fila de propostas do Workspace também inclui prioridades da equipa preparadas pelo Head Coach, com atalho para a revisão em Semana e evolução; a fila já incluía propostas ligadas à continuidade treino→treino.
Auditoria local das operações MCP: as escritas de presenças, notas factuais, avaliações, início/controlo de cronómetros, alinhamento inicial, disponibilidade/plantel e criação/duplicação de treinos agora exigem confirmação explícita no schema e no handler. As alterações existentes exigem UUID/external_key sem ambiguidade e revisão lida; a eliminação do atleta usa a RPC auditada de soft delete em vez de atualizar a tabela diretamente. Criar treino ligado a jogo também revalida a revisão da origem. Testes específicos cobrem omissão de confirmação, conflitos e isolamento de equipa. Não houve publicação das Edge Functions.
Continuação da auditoria MCP: eventos do jogo, posse e evidências de vídeo também validam confirmação no handler, mesmo quando a chamada contorna a validação do schema. A suíte cobre chamadas diretas sem confirmação.
Correção do erro reportado pelo treinador (`invalid input syntax for type uuid: "default"`): a consolidação inicial agora valida `sync_id` antes de tratar o identificador como UUID, repara os sentinelas locais sem versão remota e deixa identidades previamente confirmadas para reconciliação explícita. Regressão específica para `consolidateNow()` adicionada. Verificação: `npm run check`, `npm test` (261 aprovados, 1 ignorado), 47 testes do sincronizador e `npm run test:supabase-local` (duas sessões RLS + PWA offline/religação) passaram. O teste no Supabase publicado, no telemóvel físico e a publicação da correção continuam pendentes.
Relatórios MCP: `get_match_report` acrescentado como consulta apenas de leitura para ficha de jogo e relatório pós-jogo; reúne fontes persistidas e proveniência, não sintetiza factos ausentes nem escreve no registo. Tem testes de equipa, UUID/external_key, proveniência, períodos de utilização, ausência explícita de dados e no-write. Validação: `npm run check`, `npm test` (264 aprovados, 1 ignorado) e 3 testes específicos MCP passaram. A operação ainda não foi publicada no endpoint remoto.
Adicionada prova automatizada do sincronizador com duas bases locais isoladas e um backend partilhado em memória: criação, edição e ida/volta de lances, minutos, treinos, presenças, memórias e propostas; eliminação sem ressurreição; conflito de revisão; media privada; e serialização de sync simultânea com nova passagem para recolher alterações ocorridas durante a primeira. A troca de equipa agora espera a sync atual terminar antes de alterar a seleção, evitando que o resultado e os conflitos dessa sync sejam atribuídos ao workspace seguinte. Falhas transitórias enquanto online repetem com espera progressiva limitada. A auditoria read-only do Supabase confirmou RLS ligado e bucket de media privado; `activity_log` não estava na publicação Realtime. A migração local para a incluir passou em Postgres Supabase local, incluindo lint, reexecução e consulta da publicação; continua pendente a aplicação no projeto publicado. Continua pendente a verificação PC↔telemóvel com sessões reais, rede intermitente, fotos e reabertura da PWA; testes em memória ou emulação não substituem esses aparelhos.
Extensão da prova local: jogo construído por `VisionMatchVisual` e `VisionMatchEvents` no schema atual, minutos recalculados após ida e volta, UUIDs estáveis dos atletas apesar de IDs locais diferentes, fotografia de perfil enviada/recebida nos dois sentidos e `data_url` ausente do registo remoto. A eliminação offline da fotografia mais recente usa tombstone, mantém a foto anterior do perfil e não ressuscita o ficheiro.
O teste contra Supabase local encontrou e reproduziu uma falha na policy `teams_select`: o proprietário não conseguia concluir `INSERT ... RETURNING` antes do trigger criar a membership. A migração `20260923120100_allow_team_owner_select_during_creation.sql` limita a leitura transitória à linha cujo `owner_id` coincide com `auth.uid()`. A suíte real com duas sessões confirmou a correção, isolamento RLS, conflito de versão, eliminação por tombstone e Storage privado. O novo comando `npm run test:supabase-local` recolhe credenciais apenas da stack local e bloqueia URLs não locais. Sem alterações no projeto publicado.
Extensão da integração Supabase com duas sessões: lances e minutos de jogo atravessam PC→telemóvel, enquanto presenças de treino e proposta do Head Coach fazem ida e volta; a edição da proposta regressa ao PC enquanto uma edição concorrente do jogo é apresentada como conflito, sem impedir a reconciliação do outro registo; apagar propostas offline no telemóvel e no PC aplica tombstones no backend, remove-as no outro dispositivo e não as ressuscita em sincronizações seguintes. A mesma execução real verifica fotos privadas: apagar offline a fotografia de perfil mais recente apaga o objeto remoto, mantém a fotografia anterior e atualiza o perfil nos dois dispositivos simulados; apagar depois essa foto anterior no PC remove-a do telemóvel e limpa o perfil em ambos, sem ressurreição. A integração passou no Supabase local; continua sem substituir teste numa PWA instalada em aparelhos físicos.
Cobertura E2E da PWA: uma sessão sintética autenticada contra Supabase local cria equipa e jogo, o jogo permanece na IndexedDB com o mesmo UUID após reload offline e, ao reconectar, sincroniza para `workspace_records` uma única vez; `npm run test:supabase-local` executa este percurso junto da integração RLS em duas sessões. A configuração da app aceita HTTP apenas em loopback para desenvolvimento; URLs remotos continuam a exigir HTTPS. A validação em aparelhos reais continua pendente.
Animação, reconhecimento automático e gestão complexa multiequipa ficam para depois do núcleo.

Auditoria de publicação (23/09/2026, apenas leitura): `origin/main` continua em `c5a034b` (PR #41) e não há PRs abertos nem releases GitHub. A última execução bem-sucedida de GitHub Pages foi para esse SHA, em 22/09/2026; o service worker da publicação declara v67. O projeto Supabase Vision Coach está ativo; a Edge Function `vision-coach-mcp` está na versão 7 e o último migration remoto é `20260922181534`. O código remoto do MCP importa a revisão `c5a034b` revista no PR #41. As migrações locais `20260923120000` e `20260923120100`, bem como as novas operações MCP e módulos desta branch, ainda não estão publicados. O worktree em validação usa service worker v97. Não foram feitas escritas no Supabase, PRs, releases ou deploys.

Correções nesta continuação: a fila “Propostas por rever” abre agora a ficha de continuidade treino→treino (`#/continuidade/:id`), onde o treinador pode editar ou decidir explicitamente, em vez de abrir a consulta dos exercícios. A chave de reload do service worker usa v97 em vez de continuar presa a v95; o teste confirma tanto a preservação de uma sessão ativa como o reload disponível depois da atualização anterior. O contrato browser do agente subiu para v2: só cria briefs em rascunho, edições exigem `expected_updated_at`, e a operação compatível de memória só guarda hipóteses confirmadas com citações verificadas e identidade idempotente. Validação final: `npm run check` passou; `npm test` teve 251 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` teve 79 aprovados; `npm run test:supabase-local` passou os testes RLS/sincronização e o E2E PWA offline; `git diff --check` passou sem erros de whitespace.

Auditoria MCP complementar (23/09/2026): uma chamada direta a `record_match_event`, `update_match_event`, `delete_match_event` ou `save_match_possession` podia apresentar `expected_updated_at` correto e `expected_revision` antigo, porque o handler substituía a revisão recebida pela atual antes de aplicar o comando. O handler agora compara a revisão fornecida com `match_events.revision` antes de qualquer alteração; o teste MCP reproduz a chamada com timestamp igual e revisão antiga e confirma que é recusada. Revalidados `npm run check`, `npm test` (251 aprovados, 1 ignorado), `npm run test:e2e` (79 aprovados) e `npm run test:supabase-local` (RLS/sincronização real na stack local e PWA offline/reconexão), todos sem falhas. `origin/main` foi atualizado por fetch só de leitura e continua em `c5a034b`; não há PR aberto. A leitura atual do Supabase confirma `vision-coach-mcp` v7 e última migração remota `20260922181534`; as migrações e funções desta branch continuam sem publicação. Não houve escrita remota nem alteração das imagens aprovadas. A prova com duas sessões autenticadas em Supabase local permanece distinta da auditoria em dispositivos físicos, ainda pendente.

Correção adicional na validação de evidências para Jev: `evaluate_cross_session_relation` agora relê os registos de jogo e treino diretamente por UUID dentro da equipa e monta a citação a partir dos dados atuais. A validação já não depende do recorte de 50 fontes recentes nem do limite de 25 itens de evidência textual de `get_cross_session_evidence`. Um teste MCP com ambas as fontes fora da janela recente confirma a citação, a revisão atual, a probabilidade tipada e nenhuma escrita. A API TypeSafe documenta `POST /v1/systemone`, `jev-latest` e a resposta `answers.<id>.noul`; o formato usado no handler coincide com esse contrato. `TYPESAFE_API_KEY` não está no ambiente local; não foi lido nem alterado nenhum segredo, e a chamada real continua pendente da configuração segura no runtime Edge e da revisão antes da publicação.

Regressão desta alteração (23/09/2026): `node --test tests/match_analysis_mcp.test.js` passou 12/12; `npm run check` passou; `npm test` passou com 252 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` passou 79/79; `npm run test:supabase-local` passou a integração RLS/sync e a PWA offline/religação; `git diff --check` passou. A chamada live de Jev, os testes em dispositivo/impressora físicos e a publicação continuam por validar.

Correção de evidência de vídeo e atualização PWA (23/09/2026): a análise e o PDF agora abrem o segundo marcado, substituindo um `t` antigo e preservando os outros parâmetros do URL. A regressão Playwright inicial detetou que o cache service worker subia para v98 enquanto o registo em `index.html` e a chave de reload ficavam em v97; alinhados para v98 e acrescentada verificação do contrato de versão. Verificação final: `npm test` 253 aprovados/1 ignorado; `npm run check`; `npm run test:e2e` 79/79; `npm run test:supabase-local` RLS/sync e PWA offline/reconexão; `git diff --check`. Imagens aprovadas inalteradas. A auditoria em dispositivos físicos, chamada live do Jev e publicação continuam pendentes.

Auditoria do quadro tático MCP: o guia listava oito nomes apesar de anunciar nove operações; acrescentado `edit_match_tactics`, incluindo confirmação para adição de marcações e para remoção/limpeza. A comparação schema-handler detetou que o schema exigia `confirmed: true`, mas uma chamada direta ao handler ainda permitia adicionar cone/seta sem confirmação. O handler agora aplica o mesmo requisito; o teste de regressão tenta `add_cone` e `delete` sem confirmação e verifica que não há escrita. Verificação: 30 testes de modelo/MCP visual, Playwright visual 12/12, `npm run check`, suíte geral 252 aprovados/1 ignorado e `git diff --check`, tudo sem falhas. Não houve alteração de dados publicados nem de imagens aprovadas.

Regressão de links de vídeo (23/09/2026): análise e PDF agora apontam para o segundo exato do momento mesmo quando o URL já tinha outro parâmetro `t`; os restantes parâmetros e fragmento são preservados. Cobertura: helper de URL 3/3, Playwright análise/relatórios 13/13, suíte geral 253 aprovados/1 ignorado. A cache da PWA avança de v97 para v98 para distribuir o código atualizado; imagens aprovadas permanecem inalteradas. Ainda a fechar: verificação física em telemóvel e impressora; sem publicação.

Modo de jogo — segunda parte (23/09/2026): o estado de utilização regista a parte e o instante decorrido em que o treinador iniciou a 2.ª parte. No intervalo, o treinador escolhe entre retomar a 1.ª parte e iniciar a 2.ª; esta última ação pede confirmação e retoma o relógio, sem contar o intervalo nem reiniciar minutos. Depois da segunda parte, retomar continua identificado como 2.ª parte. O MCP `control_match_usage` oferece a ação correspondente com confirmação explícita; o estado fica no mesmo JSON sincronizado do jogo, sem nova tabela ou migração. Cache PWA v99. Verificação: testes de modelo/MCP 32/32, Playwright do quadro 13/13, `npm test` 255 aprovados/1 ignorado, `npm run check`, suíte E2E 80/80, `npm run test:supabase-local` RLS/sync e PWA offline/religação. `git diff --check` passou. O trabalho continua local, imagens aprovadas inalteradas e sem publicação. Continua pendente a auditoria em telemóvel físico.

Correção na cadeia análise→memória→Head Coach: causas prováveis de golos sofridos só são expostas como hipótese com evidência quando o lance de origem ainda existe e é do tipo golo sofrido. Se o lance for apagado ou o identificador for reutilizado por outro tipo de evento, o texto antigo da análise continua preservado, mas não é reapresentado como evidência atual na memória nem no MCP. Testes de regressão no modelo e MCP; cache PWA v100.

Integridade vídeo→evidência: o modelo local também rejeita ligações para estatísticas desconhecidas, campos de problema vazios, tipos de ligação inválidos e referências sem UUID estável. O formulário já validava a pertença das observações ao jogo e o MCP continua a repetir essa verificação. Testes de modelo/MCP e E2E; cache PWA v101.

Correção do erro Supabase `invalid input syntax for type uuid: "default"`: a sincronização agora reconhece IDs locais que não são UUID antes de os usar em filtros remotos. Um registo sem versão remota confirmada recebe um UUID novo e fica marcado para sync; se já há versão remota ou tombstone, o trabalho é preservado como conflito para reconciliação. Cobertura unitária, mensagem nas Definições e teste de duas sessões contra Supabase local; cache PWA v102. Nenhuma escrita foi feita no projeto publicado.
Seguimento após a v68 publicada: abrir uma nova aba retirou o erro UUID e revelou `IDBObjectStore.get: parameter is not a valid key`. Uma referência UUID já remota estava a ser convertida para chave numérica local (`NaN`). O hotfix isolado v69 valida UUIDs na equipa remota e preserva referências locais inválidas como conflitos, sem bloquear outros registos. A mesma correção foi aplicada a este branch maior; 47 testes específicos de sincronização passaram. A v69 foi publicada no PR #43. O treinador confirmou que a consolidação funcionou no PC; a leitura agregada do Supabase mostrou registos de jogos, atletas e memória atualizados em 23/09/2026. A verificação no telemóvel e a auditoria bidirecional completa continuam pendentes.
