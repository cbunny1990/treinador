# Próximas fases Vision Coach — 22/09/2026

Continuação local (24/09/2026, branch `feature/rag-runtime-validation-20260924` sobre `origin/main`): o contexto de planeamento RAG conta apenas treinos concluídos como trabalho realizado, exclui o próprio plano alvo da utilização recente de exercícios e assinala duração/exercícios do treino alvo quando ausentes. Commits locais `0e9662e` e `116a1a2`; sem publicação. `npm run check`, `npm test` (376 aprovados, 4 integrações locais opcionais ignoradas), Playwright completo 154/154 e regressões WebKit de fotos 3/3 passaram. A suíte WebKit cobre fila local, gravação/sincronização de fotografia grande sem duplicação e fluxo sem `createImageBitmap`; não substitui teste no telemóvel físico. O PR #53 que corrigiu a sincronização da foto móvel permanece no `origin/main` e publicado na v161; não houve novo deploy nem alteração remota nesta continuação.

Revalidação integrada RAG/sincronização (24/09/2026): a stack local padrão foi recusada pelo preflight por histórico divergente, sem aplicar migrations ou reset. Usei a cópia descartável `vision-coach-validation-20260924-run2` (26 migrations, portas próprias): integração Supabase/Auth/RLS/sincronização/RAG 3/3, E2E PC↔PWA offline/religação/conflito/tombstone 1/1 e HTTP Edge MCP 1/1 (`provider_not_configured`, sem envio externo). Corrigi `scripts/test-supabase-local.mjs` para executar o Playwright local fixado no lockfile diretamente com Node, evitando resolução acidental por `npx`; runner focado 4/4. `npm run check`, `npm test` (376 aprovados, 4 integrações locais opcionais ignoradas), Playwright completo 148/148 e `git diff --check` passaram. A stack descartável foi parada com backup dos volumes; nenhuma migration, dado, secret ou função foi alterada no projeto partilhado; sem publicação.

## Publicação de teste e auditoria remota (24/09/2026)

Atualização (24/09/2026): a v152 foi publicada após autorização explícita. PR #52, commit `563e4a24bf1a43f8b9bd10ab6cb9608ffd926c12`; CI completo passou e Pages confirmou a versão 152. O Head Coach/IA ainda não recebeu a camada RAG no Supabase remoto.

Revalidação do branch isolado (24/09/2026): `origin/main` continua em `563e4a24bf1a43f8b9bd10ab6cb9608ffd926c12`; esta branch mantém commits locais por integrar e não foi publicada. Após o commit local `0de4a94`, a pergunta com termos de saúde reconhecidos não chega ao provider de embeddings; o contexto de treino continua a incluir disponibilidade estruturada e assinala que a evidência semântica foi suprimida. `npm run check` passou; `npm test` passou com 371 testes aprovados e 4 integrações locais opcionais ignoradas; os testes RAG passaram 24/24; Deno 2.5.6 verificou a Edge Function MCP. Playwright 147/147 e o benchmark do Workspace (15 006 registos sintéticos, CPU 4×: 6,30 ms no resumo e 5,70 ms no snapshot) são da validação anterior; esta alteração não tocou na UI. A auditoria remota read-only confirma 18 migrations aplicadas, oito pendentes, `vector` disponível mas não instalada e `vision-coach-mcp` v7. RAG remoto, testes em PC/telemóvel físicos e impressão física continuam por validar; sem escrita remota nesta revalidação.

Incremento local — retenção do scroll do Workspace (24/09/2026): a atualização da vista volta à posição lida mesmo que o browser ajuste o scroll por mudança de layout; scroll posterior do treinador, incluindo interação com a barra, tem precedência. O E2E reproduziu o ajuste automático e confirmou que a versão antiga falhava, e cobre também scroll do utilizador antes do próximo frame. Avancei a shell PWA de v156 para v157; os originais aprovados continuam na cache separada sem reprocessamento. `npm run check`, `npm test` (371 aprovados, quatro integrações locais opcionais ignoradas) e Playwright Workspace/mobile/service-worker 58/58 passaram. Sem alteração de dados, imagens ou publicação.

Incremento de privacidade RAG (24/09/2026): o servidor redige nomes completos e partes reconhecíveis do nome do atleta da pergunta e dos rótulos dos chunks antes do provider de embeddings e da pesquisa lexical; títulos com termos de saúde também impedem indexação do chunk. Se a leitura de nomes falhar, a pesquisa falha fechado. O MCP devolve apenas a pergunta sanitizada; filtros UUID e excertos não mudam. A lista de nomes é reutilizada apenas dentro do mesmo pedido MCP para não repetir consultas no contexto híbrido. `npm run check`, `npm test` (375 aprovados, quatro integrações locais opcionais ignoradas), RAG 28/28 e Deno 2.5.6 passaram. Sem provider real, deploy ou envio de dados desportivos.

Revisão das oito migrations pendentes (24/09/2026): a ordem local coincide com as oito versões ausentes no projeto (`20260923120000` a `20260924120000`), anteriores à RLS interna já aplicada em `20260924144849`; o teste das migrations RAG em PostgreSQL WASM passou 4/4. As três primeiras migrations publicam alterações de atividade no Realtime, permitem ao proprietário ler a equipa que acabou de criar e anunciam que `put_record` genérico só deve escrever `game_model`; a Edge Function desta branch também rejeita no handler os outros tipos antes da RPC. A leitura read-only dos fontes publicados confirmou `head-coach-gateway` v1 ainda encaminha `put_record` sem essa allowlist, e `vision-coach-mcp` v7 importa uma revisão antiga. Portanto, aplicar só as migrations não impõe a restrição nem ativa RAG; a revisão/ativação tem de incluir os Edge Functions atualizados e validar o caminho MCP. As cinco migrations seguintes criam o índice/fila RAG, filtros multi-jogo e invalidação/reindexação ao mudar escalão ou identidade do atleta. O backfill inicial cria apenas trabalhos na fila, sem copiar textos para outro serviço; depois de Edge/provider serem configurados, o indexador envia apenas texto allowlisted para embeddings. Não encontrei escrita ampla para `anon`/`authenticated` nas tabelas privadas RAG; as RPCs são limitadas a `service_role`. Revisão estática e testes locais não substituem revisão do treinador; nada foi aplicado ao projeto partilhado.

Comparação restante dos Edge Functions (24/09/2026): `vision-coach-connectors` v2 coincide com o ficheiro local após normalização de finais de linha. `head-coach-gateway` v1 difere: o source remoto não contém `GENERIC_PUT_RECORD_KINDS`, que a branch aplica antes de chamar a RPC. `vision-coach-mcp` v7 continua a importar a revisão fixa `c5a034b47f0b5d7f408694913428da80945ba99a` do PR #41, sem apontar para o MCP RAG atual. Os endpoints auxiliares remotos de transferência de imagem, pré-visualização de imagem e pré-visualização de continuidade respondem `410` sem acesso ao workspace. A configuração local mantém JWT obrigatório no gateway/connectors e autenticação própria no MCP (`verify_jwt = false`); conservar estes valores na atualização. Nada foi publicado.

Incremento local nesta branch (cache PWA v153): a eliminação definitiva de atleta arquiva antes os objetivos individuais e as suas revisões num documento idempotente do Workspace, consultável em **Equipa → Histórico de atletas**. O arquivo não copia fotos nem URLs/media privados; UUIDs de origem, presenças e alinhamentos permanecem referenciados. Erros de identidade ou conflito no arquivo impedem a eliminação. `player_archive.js` está incluído na shell offline; E2E confirma leitura online e offline. `npm run check`, `npm test`, Playwright geral e E2E dirigidos passaram. Uma stack Supabase temporária e isolada passou `db lint` (`public`, `private`), RLS/Realtime, sincronização bidirecional de análise pós-jogo/evidências e a sincronização do novo `player_archive`; teste HTTP MCP confirmou `provider_not_configured` sem envio externo. Stack antiga e projeto Supabase partilhado não foram alterados.

Incremento local RAG (24/09/2026): teste Postgres WASM com vetores sintéticos valida em português a recuperação híbrida de paráfrase sem sobreposição lexical e correspondência lexical exata, ambas acima de um distrator. Não é uma avaliação do modelo de embeddings real; essa medição exige provider ativo e perguntas/relevância revistas pelo treinador. `node --test tests/team_knowledge_migrations.pglite.test.js` passou 3/3.

Validação local atualizada: numa stack Supabase descartável independente (`vision-coach-validation-20260924`), `db lint` passou em `public` e `private`; Auth/PostgREST, Realtime/RLS, RAG com isolamento entre equipas passaram 3/3; E2E PC↔PWA offline/religação/conflito/tombstone passou 1/1; Edge Function MCP HTTP passou 1/1, confirmando `provider_not_configured` sem envio externo. A stack antiga, com histórico divergente, foi mantida intacta. Só a migration RLS autorizada foi aplicada remotamente; as migrations RAG, Edge atualizada e provider não foram publicados/configurados.

O PR #51 foi integrado no commit `4bc40ef2043243cae125b041cbb7ee4d33a806f3`; GitHub Pages confirmou `built`, a app serviu o cache PWA `vision-coach-v151` e o Workspace carregou em modo de leitura os dados remotos atuais. Nenhum registo, migration, secret ou imagem foi alterado nesta verificação. O projeto Supabase continua com 17 migrations, a extensão `vector` disponível mas não instalada, e `vision-coach-mcp` na versão 7; portanto a camada RAG ainda não está ativa no backend.

Auditoria e remediação (24/09/2026): com autorização explícita do treinador, foi aplicada `enable_private_internal_table_rls` ao projeto `rsydvhbsxzdoprekefij`, registada como migration `20260924144849`. `private.agent_request_log` e `private.mcp_connector_tokens` têm RLS ativo e zero privilégios de tabela para `anon`/`authenticated`; `service_role` mantém leitura para o backend. Advisor confirma RLS sem policies (intencional nestas tabelas internas) e mantém um aviso separado de password leak protection desativada. O ficheiro local corresponde ao mesmo ID de migration.

Correção da PWA (24/09/2026): o bloqueio de reload mantém um marcador por sessão de treino/jogo na aba, mesmo depois de navegar para outra rota; o marcador só sai quando a sessão termina ou deixa de existir. PR #52 publicou a cache v152 depois de `npm run check`, `npm test` e Playwright 142/142 passarem no CI. Nenhum dado desportivo ou imagem aprovada foi alterado.

## Camada RAG do Head Coach — implementação local em revisão

O plano de integração está em [`team-knowledge-rag.md`](team-knowledge-rag.md). Reaproveita `workspace_records` e o MCP autenticado, com pesquisa lexical+pgvector, chunks com provenance/revisão, trigger/fila incremental e filtros por equipa/data/jogo/treino/atleta/tipo. O conteúdo estruturado continua nas consultas determinísticas. Pesquisa não cria memória, hipótese, plano nem decisão.

Estado da implementação RAG: migrations e Edge Function estão completas e validadas localmente, incluindo a stack independente descrita no topo. Em produção, só foi aplicada a migration interna RLS com autorização; as migrations RAG, extensão `vector`, Edge Function atualizada e configuração segura do `OPENAI_API_KEY` continuam por ativar. Portanto RAG segue inativo no ambiente partilhado.

Compatibilidade da ordem de migrations (24/09/2026): a migration RLS ficou registada remotamente em `20260924144849`, depois dos oito ficheiros locais ainda pendentes (`20260923120000`–`20260924120000`). Numa stack descartável reproduzi 17 migrations-base já aplicadas e a RLS aplicada antes dessas oito; `supabase db push --local --include-all` aplicou todas as oito pela ordem de timestamp. Depois, `db lint`, Auth/PostgREST/RLS/RAG 3/3, E2E sync 1/1 e Edge HTTP 1/1 passaram. Para eventual ativação, primeiro rever `db push --include-all --dry-run`; nenhuma destas oito foi aplicada ao projeto partilhado.

Esta branch usa `text-embedding-3-small` server-side para pesquisa multilingue em português. Sem `OPENAI_API_KEY`, o MCP não transmite conteúdo ao provider nem declara RAG ativo. A stack descartável valida Auth, PostgREST, `db lint`, pgvector, Edge HTTP e isolamento, sem substituir a verificação no projeto publicado. A consulta `EXPLAIN` em 2 000 chunks sintéticos usou o índice B-tree por equipa (1,386 ms medidos, zero rows removed).

Consulta de jogos recentes para o exemplo “últimos cinco jogos”: `list_matches` aceita `date_order: desc`, põe jogos sem data válida depois dos jogos datados e desempata por UUID, mantendo a ordem crescente anterior por omissão. A pesquisa RAG também aceita até dez UUIDs de jogo numa única consulta (`match_refs`), validando cada identificador e mantendo o filtro dentro da equipa autorizada. Verificação HTTP local percorreu `list_matches` → `search_team_knowledge` com os cinco UUIDs devolvidos entre seis jogos datados e um sem data; a integração Supabase aplicou a nova migration, pesquisou dois jogos da mesma equipa numa chamada e excluiu o terceiro jogo/equipa. `db lint` local, suíte Supabase (RLS/sync, Realtime, RAG e offline/religação), Playwright geral (109/109) e `npm test` passaram.

Novo incremento local: `get_training_planning_context` junta para uma data o treino agendado, duração, escalão/modelo, disponibilidade operacional, cinco jogos concluídos anteriores, três treinos recentes, exercícios usados e evidências RAG separadas por jogo e por material recente. Expõe UUIDs/revisões e flags de dados ausentes, não inclui diagnósticos, não cria plano e exige scope `read`. Filtros temporais mantêm elegíveis documentos sem data própria, como exercícios e princípios de jogo. Teste unitário cobre plantel/últimos jogos/treinos/exercícios, duas pesquisas RAG e ausência de escrita; teste HTTP local cobre a operação com provider desligado. `npm run check`, `npm test` (335 aprovados, 4 integrações locais ignoradas) e 24 testes dirigidos passaram. A stack descartável não chegou a iniciar: o CLI ficou bloqueado durante inspeção de containers Docker; por isso falta repetir `db lint`, a integração Postgres/pgvector do documento sem data e o HTTP Edge com o novo fluxo. Nenhuma publicação ou escrita na base partilhada foi feita.

Extensão RAG de adversário (24/09/2026): o indexador inclui agora notas pré-jogo, sistema/estilo observados, pontos fortes, vulnerabilidades e aspetos a observar, mantendo o plano de jogo como decisão do treinador e citando o mesmo UUID e revisão da ficha do jogo. Regressão verifica categorias e proveniência; nenhuma observação é gerada pela IA.

Cobertura RAG de notas livres de treino (24/09/2026): acrescentadas as notas gerais do treinador guardadas no plano existente, com `training_ref`, data e categoria de observação. Validação após este ajuste: `npm run check`; `npm test` (337 aprovados, 4 integrações locais ignoradas); 26 testes RAG/schema dirigidos; `git diff --check`. Playwright 109/109 passou após o incremento de contexto híbrido anterior; as alterações mais recentes são só Edge/índice e não mudam a PWA. O Docker Desktop devolve HTTP 500 no endpoint do engine Linux para as versões de API 1.55 e 1.44, pelo que a aplicação/lint da migration local permanece pendente.

O contexto híbrido restringe agora as queries estruturadas: sessão do dia, treinos/jogos anteriores ordenados por data, modelo mais recente e apenas exercícios citados pelos treinos selecionados. Um teste MCP verifica os filtros/limites e que não carrega a biblioteca inteira. Ordenar campos JSON por caminho é suportado pelo PostgREST usado pelo Supabase.

Limite de transferência do contexto híbrido (24/09/2026): queries agora filtram a sessão por dia e recortam as sessões/jogos anteriores no servidor; o modelo é limitado ao mais recente e os exercícios à UUID dos blocos usados. Regressão confirma os filtros JSON, ordenação/limite e conjunto de exercícios. `npm run check`, `npm test` (337 aprovados, 4 integrações locais ignoradas), 26 testes MCP/schema e `git diff --check` passaram. Postgres/Edge real continuam por validar enquanto Docker Desktop devolve HTTP 500; sem alterações na PWA ou produção.

Continuação RAG (24/09/2026): a RPC de pesquisa multi-jogo aceita agora `per_match_limit`, validado apenas com UUIDs explícitos de jogos, para limitar excertos por jogo depois da fusão semântica+lexical. `get_training_planning_context` pede dois excertos por cada um dos cinco jogos recentes, reduzindo a hipótese de um jogo dominar a evidência. MCP orienta a sequência estruturada `list_matches` recentes concluídos → pesquisa RAG por UUID. `npm run check`, testes dirigidos MCP/schema/RAG (46 aprovados, 2 integrações locais ignoradas) e `npm test` (338 aprovados, 4 integrações locais ignoradas) passaram. A migration desta alteração ainda requer lint e execução Postgres locais: Docker Desktop continua sem engine acessível. Sem publicação, alteração de dados ou mudança da PWA.

Auditoria/correção RAG: a RPC multi-jogo rejeita agora `match_refs: []`; chunks de objetivos e planos semanais preservam nos metadados as UUIDs de evidências/sessões relacionadas. Os testes incluem proveniência e rejeição de filtro vazio no teste de integração local pendente.

Regressão de scroll do Workspace (24/09/2026): removido o segundo restauro de posição disparado por `sync-complete`, que podia repor a posição antiga depois de o treinador arrastar a barra durante uma renderização pendente. `setView()` continua responsável por preservar o scroll na mesma rota e reinicia ao navegar. E2E cobre scroll com wheel, sem wheel/barra e atualização da página; `npm run check` e os E2E focados passaram 3/3. Continua local.

Validação geral após os incrementos (24/09/2026): `npm test` 339 aprovados, 4 integrações locais ignoradas; `npm run check` passou; Playwright completo 110/110; `git diff --check` passou. Supabase remoto inspecionado apenas em leitura: última migration aplicada `20260922181534`, MCP v7 e extensão vector disponível mas ainda não instalada; migrations/Edge RAG continuam locais. Advisor de segurança reporta password leak protection desativada; advisor de performance lista sete índices sem uso observado. Não alterei configuração, índices, dados, Edge Functions nem secrets remotos.

Auditoria RAG atualizada (24/09/2026): implementação local passou `npm run check`, `npm test` (343 aprovados, 4 integrações locais ignoradas), 36 testes dirigidos de RAG/MCP e validação estática. Playwright registou 116 aprovados e um teste flaky de conflito: timeout de arranque na primeira execução, passou na repetição e também isoladamente (1/1). Docker impediu repetir migrations/Postgres e Edge HTTP local. A última inspeção remota de leitura encontrou o projeto sem migrations/tabelas RAG, `vector` não instalado e MCP v7; nenhuma escrita remota ou configuração de provider foi feita. A mesma inspeção encontrou duas tabelas internas privadas remotas sem RLS, apesar da migration local `20260923140620_enable_private_internal_table_rls`; validar essa divergência antes de ativar as migrations pendentes.

Correção encontrada na revisão das migrations RAG (24/09/2026): trigger de indexação/remoção também dispara em `DELETE` físico. A função já tinha o ramo de limpeza, mas o trigger anterior só cobria insert/update, podendo deixar chunks/jobs órfãos. `npm run check`, `npm test` (343 aprovados, 4 integrações locais ignoradas), testes RAG/schema 30/30 e `git diff --check` passaram. A sintaxe do evento segue o `CREATE TRIGGER` do PostgreSQL 17; Docker continuou sem responder por mais de 60 segundos, por isso a execução real da migration local continua pendente.

Incremento da evolução e relatórios (24/09/2026): associações a objetivos agora separam sessões de origem (`sessions`) de sessões concluídas explicitamente assinaladas como trabalho (`worked_sessions`), sem inferência retroativa. Planos semanais mostram primeiro a semana mais recente e identificam a atual, a seguinte e as anteriores. O PDF de pós-jogo assinala referências a problemas removidos da análise atual como históricas. O emblema original do Sub-8 de Figueiró acompanha todos os tipos de PDF. Validação local: `npm run check`, `npm test` (348 aprovados, 4 integrações locais ignoradas), Playwright completo 119/119 e `git diff --check` passaram. Mudanças continuam locais; Supabase remoto e publicação não foram alterados.

Arquivo de épocas ampliado (24/09/2026): cada época mostra as sessões do intervalo com ligações às fichas, resultados registados, objetivos de equipa identificados no período ou sustentados por evidências do período, e planos semanais com avaliação explícita ou pendente. A página referencia os registos existentes sem os copiar. `npm run check`, `npm test` (348 aprovados, 4 integrações locais ignoradas), Playwright completo 120/120 e `git diff --check` passaram. Só local; não foi publicado.

Histórico de convocatória após eliminação (24/09/2026): a ficha de jogo agora recupera nome, dorsal e alinhamento do snapshot inicial quando o atleta já não existe no plantel local e marca a origem histórica; sem snapshot usa o UUID explícito, nunca inventa nome. Teste de remoção definitiva cobre convocatória/alinhamento preservados. `npm run check`, `npm test` (348 aprovados, 4 integrações locais ignoradas) e testes Playwright dirigidos 26/26 passaram. Suíte completa: 118 aprovados e três casos flaky por timeouts (calendário desktop, zonas do campo, aprovação paralela); os três passaram isolados. Nenhuma falha final; local, sem publicação.

## Fase 1 — treino completo
Implementação nesta entrega: presenças e histórico individual, iniciar/pausar/retomar/terminar, cronómetro por bloco e total, notas editáveis, resumo realizado, duplicação sem copiar execução e reordenação com notas preservadas. Guia: training-session.md.
O treino planeado é preservado. Imagens e consulta anteriores não são redesenhadas.

## Fase 2 — continuidade do Head Coach
Implementado: avaliação ligada a memória única, rascunho com evidências, revisão pela IA autorizada, aprovação explícita e ligação ao treino seguinte com resultado assinalado. Guia: training-continuity.md. O rascunho inicial é um modelo local, não análise automática de toda a época; agregação de tendências fica para aprofundamento posterior.

## Fase 3 — jogo e quadro tático
Implementado o primeiro incremento: campo 5v5, alinhamento e suplentes, plano de rotações, substituições e trocas de funções confirmadas, cronómetro e minutos registados por atleta/GR. O quadro destaca também a diferença de utilização e quem ficou abaixo de metade do tempo decorrido, em ordem alfabética e sem ranking. Guia: match-visual.md.
Incremento adicional local: editar ou anular movimentos individuais na cronologia, com auditoria e recálculo de minutos; operações MCP específicas usam as mesmas validações e recusam sequências dependentes incompatíveis. Testes locais de navegador e emulação móvel incluídos; continua pendente a verificação física de sincronização entre dispositivos.
Implementado o segundo incremento: lances do jogo com minuto, atletas próprios por UUID e identificação opcional e manual do atleta adversário por nome, lado, zona e motivo (perdas), estatísticas contadas com proveniência, posse medida/estimada/desconhecida e operações MCP. Guia: match-events.md. Implementada localmente a análise pós-jogo estruturada, ligação explícita à memória, preparação do treino seguinte e operações MCP de leitura/proposta. Guia: match-analysis.md. A preparação regista sistema, estilo, pontos fortes e vulnerabilidades observados do adversário; o campo visual guarda cones e setas táticas com edição explícita e MCP autorizado. Continuar a rever usabilidade do quadro no campo e testar sincronização em dispositivos reais.
O posicionamento visual e os lances agora partilham a mesma conversão das coordenadas do campo para três terços × três corredores. O quadro desenha as nove células, expõe a zona de cada marcador e o MCP devolve `position_zones`; a função única está em `VisionMatchEvents.zoneFromPoint`.

## Fase 4 — relatórios na app
Implementado localmente: folhas A4 imprimíveis com opção Guardar como PDF para treino (inclui imagem aprovada), ficha de jogo, relatório pós-jogo, atleta e equipa. Os relatórios de equipa e atleta usam período de época quando escolhido e incluem objetivos, avaliação semanal, prioridades e proveniência dos dados. Uma época já inexistente não alarga o relatório a todo o histórico; minutos não registados não são apresentados como zero, escalações não iniciadas não contam como titularidade, minutos registados a zero ficam distintos dos desconhecidos e objetivos associados a outras épocas identificam a evidência fora do intervalo. Estados de presença por marcar não contam como presenças. Minutos no relatório de equipa aparecem alfabeticamente, sem criar uma classificação; as posições individuais derivam dos movimentos registados e também são devolvidas na consulta MCP de utilização. O MCP agora oferece relatórios estruturados de jogo, treino, atleta e equipa (leitura, sem geração de PDF remoto). A exportação A4 foi gerada em Playwright com 100 acontecimentos e validada como multipágina; a folha pós-jogo também foi verificada em 390 px sem overflow horizontal. Guias: reports.md e seasons.md. Pendente: validação numa impressora física e em telemóveis reais.

## Fase 5 — desenvolvimento dos atletas
Implementado localmente: objetivos individuais com data, evidências, exercícios relacionados, estado explícito, histórico e operações MCP; a ficha longitudinal mostra convocações, titularidades, entradas/saídas com minuto e minutos registados, sem assumir zero quando não há utilização medida. `get_player_participation_history` expõe a mesma evolução e presenças ao Head Coach em leitura remota, com isolamento de equipa e UUID estável. Testes incluem convocação sem minutos, jogo com reentrada, histórico MCP e integração ao Supabase local. Guia: player-development.md.

## Fase 6 — semana e época
Implementado localmente: painel de semana e evolução da equipa, planos semanais com referências estáveis a treinos/jogo, nomes e ligações às fichas, relação entre sessões e avaliação explícita; objetivos de equipa com fluxo identificado → planeado → trabalhado → observado → avaliado → melhorou/continua, observação/interpretação/hipótese/avaliação/decisão separados, sessões, exercícios e evidências com ligação às origens, e operações MCP. Estados avaliados exigem avaliação e decisão do treinador, sem avanço por inferência. Arquivo de épocas com plantel por UUID estável, ativação/arquivo sem apagar histórico, contagens por período e relatórios de atleta filtrados por data; operações MCP com revisão e confirmação. Guias: team-development.md e seasons.md.
Revisão da evolução: distingue sessões relacionadas das sessões concluídas que o treinador confirma como trabalho do objetivo; não retroconverte associações antigas nem presume trabalho. A ficha e o MCP validam a conclusão e preservam referências na edição. Semanas ordenam-se por data descendente e assinalam a atual, a seguinte e as anteriores. PDF pós-jogo identifica como referência histórica um problema associado que já não consta da análise atual. Cobertura Playwright acrescentada para ordenação semanal; publicação e validação remota continuam pendentes.

## Fase 7 — vídeo
Implementado localmente: URL, timestamp, categoria, atleta opcional, descrição e ligações tipadas a observações do mesmo jogo, estatísticas e problemas com conteúdo na análise; edição/apagamento, sincronização pelo jogo e validação MCP com UUIDs estáveis. Guia: match-video-evidence.md.

Novo incremento local: `get_recurring_match_patterns` agrega perdas explicitamente registadas em jogos ativos, por motivo e zona, e devolve UUIDs e IDs de eventos de origem. Exige repetição em pelo menos dois jogos; não transforma notas ou hipóteses em factos nem recomenda prioridades. Teste MCP de leitura e contagem incluído. A consulta jogo↔treino equilibra evidências das duas fontes. `evaluate_cross_session_relation` revalida duas citações exatas e pede ao Jev uma probabilidade Noul de apoio semântico, sem escrita ou decisão automática; limita a avaliação à afirmação e às citações escolhidas. Testes usam fetch simulado, incluindo repetição limitada com `Retry-After` para respostas 429/529. Em 23/09/2026 confirmei com `supabase secrets list` (CLI 2.117.0) que `TYPESAFE_API_KEY` não está configurada no projeto publicado; não foram consultados ou expostos valores de segredos. A chamada live fica indisponível até existir configuração autorizada no runtime Edge, depois ainda requer validação real e revisão antes da publicação.

Continuação: `get_cross_session_evidence` fornece ao Head Coach leituras recentes de análises/lances de jogos e avaliações/notas de treinos, com classificação por facto registado, observação, interpretação, hipótese, avaliação ou decisão do treinador. Inclui referências UUID e sinaliza truncagem; não grava memória nem proposta. Testes MCP cobrem as categorias e a leitura sem escrita. Permanece necessária a revisão do treinador a qualquer relação semântica ou prioridade gerada pelo Head Coach.

Novo fluxo local: `prepare_cross_session_priority` valida citação, UUID, pertença à equipa e versão atual de cada fonte; guarda o objetivo como proposta do Head Coach na coleção de documentos existente e evita duplicados por identidade/idempotência. Uma nova versão da fonte gera identidade de proposta própria. A ficha mostra fontes e permite editar interpretação/hipótese. Aceitar requer decisão escrita e confirmação do treinador; rejeitar também confirma e preserva histórico. Nenhuma ação cria treino. MCP específico de preparação/aceitação/rejeição e testes de conflito incluídos.

## Transversal
Implementada localmente a pesquisa por atleta, adversário, treino, jogo/lance, exercício, objetivo, observação, documento e media, com filtros de data/época e ligação à origem. Guia: search-history.md.
Correção de UX nesta continuação: a fila de propostas do Workspace também inclui prioridades da equipa preparadas pelo Head Coach, com atalho para a revisão em Semana e evolução; a fila já incluía propostas ligadas à continuidade treino→treino.
Auditoria local das operações MCP: as escritas de presenças, notas factuais, avaliações, início/controlo de cronómetros, alinhamento inicial, disponibilidade/plantel e criação/duplicação de treinos agora exigem confirmação explícita no schema e no handler. As alterações existentes exigem UUID/external_key sem ambiguidade e revisão lida; a eliminação do atleta usa a RPC auditada de soft delete em vez de atualizar a tabela diretamente. Criar treino ligado a jogo também revalida a revisão da origem. Testes específicos cobrem omissão de confirmação, conflitos e isolamento de equipa. Não houve publicação das Edge Functions.
Continuação da auditoria MCP: eventos do jogo, posse e evidências de vídeo também validam confirmação no handler, mesmo quando a chamada contorna a validação do schema. A suíte cobre chamadas diretas sem confirmação.
Correção do erro reportado pelo treinador (`invalid input syntax for type uuid: "default"`): a consolidação inicial valida `sync_id` antes de tratar o identificador como UUID, repara os sentinelas locais sem versão remota e deixa identidades previamente confirmadas para reconciliação explícita. Regressão específica para `consolidateNow()` adicionada. A correção foi publicada em duas etapas, v68 e v69; a consolidação e a leitura dos dados recentes no telemóvel foram confirmadas pelo treinador.
Relatórios MCP: `get_match_report` acrescentado como consulta apenas de leitura para ficha de jogo e relatório pós-jogo; reúne fontes persistidas e proveniência, não sintetiza factos ausentes nem escreve no registo. Tem testes de equipa, UUID/external_key, proveniência, períodos de utilização, ausência explícita de dados e no-write. Validação: `npm run check`, `npm test` (264 aprovados, 1 ignorado) e 3 testes específicos MCP passaram. A operação ainda não foi publicada no endpoint remoto.
Adicionada prova automatizada do sincronizador com duas bases locais isoladas e um backend partilhado em memória: criação, edição e ida/volta de lances, minutos, treinos, presenças, memórias e propostas; eliminação sem ressurreição; conflito de revisão; media privada; e serialização de sync simultânea com nova passagem para recolher alterações ocorridas durante a primeira. A troca de equipa agora espera a sync atual terminar antes de alterar a seleção, evitando que o resultado e os conflitos dessa sync sejam atribuídos ao workspace seguinte. Falhas transitórias enquanto online repetem com espera progressiva limitada. A auditoria read-only do Supabase confirmou RLS ligado e bucket de media privado; `activity_log` não estava na publicação Realtime. A migração local para a incluir passou em Postgres Supabase local, incluindo lint, reexecução e consulta da publicação; continua pendente a aplicação no projeto publicado. Continua pendente a verificação PC↔telemóvel com sessões reais, rede intermitente, fotos e reabertura da PWA; testes em memória ou emulação não substituem esses aparelhos.
Extensão da prova local: jogo construído por `VisionMatchVisual` e `VisionMatchEvents` no schema atual, minutos recalculados após ida e volta, UUIDs estáveis dos atletas apesar de IDs locais diferentes, fotografia de perfil enviada/recebida nos dois sentidos e `data_url` ausente do registo remoto. A eliminação offline da fotografia mais recente usa tombstone, mantém a foto anterior do perfil e não ressuscita o ficheiro.
O teste contra Supabase local encontrou e reproduziu uma falha na policy `teams_select`: o proprietário não conseguia concluir `INSERT ... RETURNING` antes do trigger criar a membership. A migração `20260923120100_allow_team_owner_select_during_creation.sql` limita a leitura transitória à linha cujo `owner_id` coincide com `auth.uid()`. A suíte real com duas sessões confirmou a correção, isolamento RLS, conflito de versão, eliminação por tombstone e Storage privado. O novo comando `npm run test:supabase-local` recolhe credenciais apenas da stack local e bloqueia URLs não locais. Sem alterações no projeto publicado.
Extensão da integração Supabase com duas sessões: lances e minutos de jogo atravessam PC→telemóvel, enquanto presenças de treino e proposta do Head Coach fazem ida e volta; a edição da proposta regressa ao PC enquanto uma edição concorrente do jogo é apresentada como conflito, sem impedir a reconciliação do outro registo; apagar propostas offline no telemóvel e no PC aplica tombstones no backend, remove-as no outro dispositivo e não as ressuscita em sincronizações seguintes. A mesma execução real verifica fotos privadas: apagar offline a fotografia de perfil mais recente apaga o objeto remoto, mantém a fotografia anterior e atualiza o perfil nos dois dispositivos simulados; apagar depois essa foto anterior no PC remove-a do telemóvel e limpa o perfil em ambos, sem ressurreição. A integração passou no Supabase local; continua sem substituir teste numa PWA instalada em aparelhos físicos.
Cobertura E2E da PWA: uma sessão sintética autenticada contra Supabase local cria equipa e jogo, o jogo permanece na IndexedDB com o mesmo UUID após reload offline e, ao reconectar, sincroniza para `workspace_records` uma única vez; `npm run test:supabase-local` executa este percurso junto da integração RLS em duas sessões. A configuração da app aceita HTTP apenas em loopback para desenvolvimento; URLs remotos continuam a exigir HTTPS. A validação em aparelhos reais continua pendente.
Animação, reconhecimento automático e gestão complexa multiequipa ficam para depois do núcleo.

Auditoria de publicação (23/09/2026, apenas leitura): o código publicado foi atualizado até à v69 pelo PR #43; os novos módulos, operações MCP e migrações desta branch continuam locais. A leitura agregada após a consolidação confirmou registos remotos de jogos, atletas e memória atualizados hoje. Esta branch usa cache PWA v109 e preserva os originais aprovados dos exercícios.

Correções nesta continuação: a fila “Propostas por rever” abre agora a ficha de continuidade treino→treino (`#/continuidade/:id`), onde o treinador pode editar ou decidir explicitamente, em vez de abrir a consulta dos exercícios. A chave de reload do service worker usa v97 em vez de continuar presa a v95; o teste confirma tanto a preservação de uma sessão ativa como o reload disponível depois da atualização anterior. O contrato browser do agente subiu para v2: só cria briefs em rascunho, edições exigem `expected_updated_at`, e a operação compatível de memória só guarda hipóteses confirmadas com citações verificadas e identidade idempotente. Validação final: `npm run check` passou; `npm test` teve 251 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` teve 79 aprovados; `npm run test:supabase-local` passou os testes RLS/sincronização e o E2E PWA offline; `git diff --check` passou sem erros de whitespace.

Auditoria MCP complementar (23/09/2026): uma chamada direta a `record_match_event`, `update_match_event`, `delete_match_event` ou `save_match_possession` podia apresentar `expected_updated_at` correto e `expected_revision` antigo, porque o handler substituía a revisão recebida pela atual antes de aplicar o comando. O handler agora compara a revisão fornecida com `match_events.revision` antes de qualquer alteração; o teste MCP reproduz a chamada com timestamp igual e revisão antiga e confirma que é recusada. Revalidados `npm run check`, `npm test` (251 aprovados, 1 ignorado), `npm run test:e2e` (79 aprovados) e `npm run test:supabase-local` (RLS/sincronização real na stack local e PWA offline/reconexão), todos sem falhas. `origin/main` foi atualizado por fetch só de leitura e continua em `c5a034b`; não há PR aberto. A leitura atual do Supabase confirma `vision-coach-mcp` v7 e última migração remota `20260922181534`; as migrações e funções desta branch continuam sem publicação. Não houve escrita remota nem alteração das imagens aprovadas. A prova com duas sessões autenticadas em Supabase local permanece distinta da auditoria em dispositivos físicos, ainda pendente.

Correção adicional na validação de evidências para Jev: `evaluate_cross_session_relation` agora relê os registos de jogo e treino diretamente por UUID dentro da equipa e monta a citação a partir dos dados atuais. A validação já não depende do recorte de 50 fontes recentes nem do limite de 25 itens de evidência textual de `get_cross_session_evidence`. Um teste MCP com ambas as fontes fora da janela recente confirma a citação, a revisão atual, a probabilidade tipada e nenhuma escrita. A API TypeSafe documenta `POST /v1/systemone`, `jev-latest` e a resposta `answers.<id>.noul`; o formato usado no handler coincide com esse contrato. `TYPESAFE_API_KEY` não está no ambiente local; não foi lido nem alterado nenhum segredo, e a chamada real continua pendente da configuração segura no runtime Edge e da revisão antes da publicação.

Regressão desta alteração (23/09/2026): `node --test tests/match_analysis_mcp.test.js` passou 12/12; `npm run check` passou; `npm test` passou com 252 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` passou 79/79; `npm run test:supabase-local` passou a integração RLS/sync e a PWA offline/religação; `git diff --check` passou. A chamada live de Jev, os testes em dispositivo/impressora físicos e a publicação continuam por validar.

Correção de evidência de vídeo e atualização PWA (23/09/2026): a análise e o PDF agora abrem o segundo marcado, substituindo um `t` antigo e preservando os outros parâmetros do URL. A regressão Playwright inicial detetou que o cache service worker subia para v98 enquanto o registo em `index.html` e a chave de reload ficavam em v97; alinhados para v98 e acrescentada verificação do contrato de versão. Verificação final: `npm test` 253 aprovados/1 ignorado; `npm run check`; `npm run test:e2e` 79/79; `npm run test:supabase-local` RLS/sync e PWA offline/reconexão; `git diff --check`. Imagens aprovadas inalteradas. A auditoria em dispositivos físicos, chamada live do Jev e publicação continuam pendentes.

Auditoria do quadro tático MCP: o guia listava oito nomes apesar de anunciar nove operações; acrescentado `edit_match_tactics`, incluindo confirmação para adição de marcações e para remoção/limpeza. A comparação schema-handler detetou que o schema exigia `confirmed: true`, mas uma chamada direta ao handler ainda permitia adicionar cone/seta sem confirmação. O handler agora aplica o mesmo requisito; o teste de regressão tenta `add_cone` e `delete` sem confirmação e verifica que não há escrita. Verificação: 30 testes de modelo/MCP visual, Playwright visual 12/12, `npm run check`, suíte geral 252 aprovados/1 ignorado e `git diff --check`, tudo sem falhas. Não houve alteração de dados publicados nem de imagens aprovadas.

Regressão de links de vídeo (23/09/2026): análise e PDF agora apontam para o segundo exato do momento mesmo quando o URL já tinha outro parâmetro `t`; os restantes parâmetros e fragmento são preservados. Cobertura: helper de URL 3/3, Playwright análise/relatórios 13/13, suíte geral 253 aprovados/1 ignorado. A cache da PWA avança de v97 para v98 para distribuir o código atualizado; imagens aprovadas permanecem inalteradas. Ainda a fechar: verificação física em telemóvel e impressora; sem publicação.

Modo de jogo — segunda parte (23/09/2026): o estado de utilização regista a parte e o instante decorrido em que o treinador iniciou a 2.ª parte. No intervalo, o treinador escolhe entre retomar a 1.ª parte e iniciar a 2.ª; esta última ação pede confirmação e retoma o relógio, sem contar o intervalo nem reiniciar minutos. Depois da segunda parte, retomar continua identificado como 2.ª parte. O MCP `control_match_usage` oferece a ação correspondente com confirmação explícita; o estado fica no mesmo JSON sincronizado do jogo, sem nova tabela ou migração. Cache PWA v99. Verificação: testes de modelo/MCP 32/32, Playwright do quadro 13/13, `npm test` 255 aprovados/1 ignorado, `npm run check`, suíte E2E 80/80, `npm run test:supabase-local` RLS/sync e PWA offline/religação. `git diff --check` passou. O trabalho continua local, imagens aprovadas inalteradas e sem publicação. Continua pendente a auditoria em telemóvel físico.

Correção na cadeia análise→memória→Head Coach: causas prováveis de golos sofridos só são expostas como hipótese com evidência quando o lance de origem ainda existe e é do tipo golo sofrido. Se o lance for apagado ou o identificador for reutilizado por outro tipo de evento, o texto antigo da análise continua preservado, mas não é reapresentado como evidência atual na memória nem no MCP. Testes de regressão no modelo e MCP; cache PWA v100.

Integridade vídeo→evidência: o modelo local também rejeita ligações para estatísticas desconhecidas, campos de problema vazios, tipos de ligação inválidos e referências sem UUID estável. O formulário já validava a pertença das observações ao jogo e o MCP continua a repetir essa verificação. Testes de modelo/MCP e E2E; cache PWA v101.

Correção do erro Supabase `invalid input syntax for type uuid: "default"`: a sincronização agora reconhece IDs locais que não são UUID antes de os usar em filtros remotos. Um registo sem versão remota confirmada recebe um UUID novo e fica marcado para sync; se já há versão remota ou tombstone, o trabalho é preservado como conflito para reconciliação. Cobertura unitária, mensagem nas Definições e teste de duas sessões contra Supabase local; cache PWA v102. Nenhuma escrita foi feita no projeto publicado.
Seguimento após a v68 publicada: abrir uma nova aba retirou o erro UUID e revelou `IDBObjectStore.get: parameter is not a valid key`. Uma referência UUID já remota estava a ser convertida para chave numérica local (`NaN`). O hotfix v69 valida UUIDs na equipa remota e preserva referências locais inválidas como conflitos, sem bloquear outros registos. A v69 foi publicada no PR #43. O treinador confirmou a consolidação no PC e a sincronização no telemóvel com os dados recentes visíveis. Isto verifica este percurso nos dois dispositivos; os restantes cenários da auditoria bidirecional, incluindo offline, edições concorrentes, eliminação e media, continuam por verificar fisicamente.

Integração local com a v69: a branch de desenvolvimento foi reunida com `origin/main`; 54 testes específicos do sincronizador passaram, bem como `npm run check`, `npm test` (271 aprovados, 1 ignorado), a suíte Playwright (81/81) e `npm run test:supabase-local` (integração RLS com duas sessões e PWA offline/reconexão). O teste MCP adicional de `get_training_report` passou com isolamento de equipa e sem escrita. Os novos módulos, migrações e Edge Functions continuam locais para revisão; a v69 publicada permanece intacta.

Correção seguinte na branch local: os planos de treino antigos com `blocos[].exercise_ref` numérico e os blocos congelados em `session.blocks` passam a enviar o UUID do exercício da mesma equipa; uma referência ausente ou de outra equipa fica como conflito explícito. A ordem de sincronização garante o envio do exercício antes do treino. O percurso exercício→plano/sessão foi verificado em dois dispositivos simulados e em duas sessões autenticadas no Supabase local; nenhum dado do projeto publicado foi alterado. Cache PWA v103. Validação: `npm run check`, `npm test` (274 aprovados, 1 ignorado), 56 testes específicos do sincronizador, `npm run test:e2e` (81/81) e `npm run test:supabase-local` passaram.

Extensão para jogos legados: convocatória e alinhamento convertem IDs numéricos locais em UUIDs de atletas da equipa. O teste entre dois dispositivos e a integração Supabase local verificam a ligação no segundo dispositivo. Jogos históricos podem continuar a referir por UUID atletas entretanto eliminados sem os tornar disponíveis para novas escolhas. A sincronização mantém conflitos explícitos se uma referência local não tiver correspondência confirmável.

Proteção de escrita concorrente local: a confirmação remota passa a usar uma transação IndexedDB que compara a cópia enviada com a cópia atual. Edições ocorridas durante o envio continuam pendentes para nova sincronização; a versão remota confirmada é guardada sem substituir o texto novo. Um tombstone pendente impede que o pull recrie uma linha apagada durante o envio; a versão da escrita recém-confirmada é usada para concluir a eliminação condicional. Testes específicos cobrem edição e eliminação nesse intervalo, incluindo IndexedDB real no navegador. Cache PWA v104; os novos módulos continuam apenas no PR em rascunho.

Verificação deste incremento: `npm run check`; `npm test` com 279 aprovados, 1 ignorado e 0 falhas; 61 testes do sincronizador; `npm run test:e2e` com 82 aprovados; `npm run test:supabase-local` com duas sessões autenticadas, RLS e PWA offline/reconexão. `git diff --check` passou. Os originais aprovados continuam intactos.

Media durante sincronização: títulos/notas editados durante o upload já não são substituídos pela confirmação remota. Os novos uploads locais usam caminho privado com SHA-256 dos bytes; substituir o conteúdo cria uma referência diferente sem modificar o ficheiro anterior. Uma fotografia cujos bytes mudaram durante o envio mantém a nova versão pendente; a eliminação pendente não é ressuscitada pelo pull. Validação: `npm run check`; `npm test` com 282 aprovados, 1 ignorado e 0 falhas; 64 testes específicos do sincronizador; 82 testes Playwright; `npm run test:supabase-local` com RLS, duas sessões e PWA offline/reconexão. Cache PWA v105, ainda apenas na branch do PR em rascunho.

Pull durante sincronização: se o treinador editar um jogo ou uma nota de media enquanto a app hidrata referências ou obtém um URL privado, a transação IndexedDB deteta a alteração antes de aplicar a cópia remota. A edição local permanece e uma versão remota divergente é apresentada como conflito; se o registo tiver sido apagado, o pull não o recria. Testes específicos de corrida em dois dispositivos simulados. Cache PWA v106, ainda apenas na branch do PR em rascunho.

Foto do atleta: a atualização automática da ligação à media passou a comparar a ficha dentro da transação local. Uma edição simultânea da ficha permanece intacta e uma fotografia local mais recente não é substituída por media anterior enquanto aguarda upload. Uma foto local sem referência gerida não é apagada pela ausência temporária de media. Validação da v106: `npm run check`; `npm test` com 287 aprovados, 1 ignorado e 0 falhas; 69 testes específicos de sincronização; 82 Playwright; `npm run test:supabase-local` com RLS, duas sessões e PWA offline/reconexão; `git diff --check` sem erros.

Eliminação remota: a IndexedDB compara a linha vista antes de apagar. Se uma edição local entrou entretanto, a eliminação é recusada e o conflito fica visível, tanto em jogos como em media. Um teste de navegador valida a transação real. Validação da v107: `npm run check`; `npm test` com 289 aprovados, 1 ignorado e 0 falhas; 71 testes específicos; 83 Playwright; `npm run test:supabase-local` com RLS, duas sessões e PWA offline/reconexão. Cache PWA v107, ainda apenas no PR em rascunho.

Media geral: a biblioteca permite agora editar tipo, título, nota e URL externo no mesmo registo. A gravação exige a revisão lida, marca o registo para sincronizar e mantém a UUID, associação, caminho privado e bytes de ficheiros. O histórico regista a edição. O MCP adiciona leitura por UUID e edição confirmada de links externos, relendo a revisão e usando RPC compare-and-set; ficheiros privados não são alterados. Validação v108: `npm run check`; `npm test` com 292 aprovados, 1 ignorado e 0 falhas; 71 testes específicos do sincronizador e 7 testes de media; 83 testes Playwright passaram na execução sequencial, incluindo edição a 390 px; `npm run test:supabase-local` com RLS, duas sessões e PWA offline/reconexão. Cache PWA v108.

Sincronização offline e conflito concorrente: duas instâncias PWA isoladas contra Supabase local confirmam criação offline no PC, edição offline no telemóvel, eliminação offline nos dois sentidos após reconexão e ausência de ressurreição. Edições offline da mesma revisão nos dois aparelhos mantêm a versão do primeiro envio no servidor, preservam a segunda versão local como pendente e expõem `version_mismatch` na lista de conflitos do Workspace. A correção do tombstone impede que uma eliminação recebida crie outro tombstone numa transação que só inclui o registo. Validação cache PWA v109: `npm run check`; `npm test` com 292 aprovados, 1 ignorado; `npm run test:e2e` com 83 aprovados; `npm run test:supabase-local` passou RLS, duas sessões e PWA bidirecional; imagens aprovadas sem alterações.

Foto do atleta entre dispositivos: a PWA do PC cria a foto enquanto está offline e, após reconectar, envia-a ao bucket privado `team-media`; a PWA do telemóvel recebe URL assinada e a UUID do atleta, sem transportar `data_url`. A edição offline do título no telemóvel sincroniza para o PC sem alterar o caminho do ficheiro. A eliminação offline propaga o tombstone e remove a cópia dos dois dispositivos. Verificado em Playwright com duas PWA e duas sessões autenticadas contra Supabase local; acesso público ao objeto recusado. `npm run test:supabase-local` passou (RLS, sincronização, conflito, tombstone, foto privada e equipa errada). O teste é local e não substitui a confirmação nos aparelhos físicos.

Validação integral do estado local `b91e8a6`: `npm run check` passou; `npm test` passou com 292 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` passou com 83/83; `npm run test:supabase-local` passou com duas sessões autenticadas, RLS, PWA de PC/telemóvel e media privada. A suíte confirma os bytes e a resolução dos originais aprovados sem os alterar. O commit está apenas no branch local do PR #44; não foi feito deploy nem aplicação de migrações no projeto publicado. Continuam pendentes a auditoria em aparelhos reais e impressora, a chamada live do Jev depois de existir configuração segura, e a revisão do treinador antes de qualquer publicação.

Reconciliação pré-deploy remoto (23/09/2026): foram recuperados os 17 ficheiros com as versões existentes no Supabase de produção; a branch acrescenta agora três migrações. Após uma falha transitória do Docker, a stack isolada ficou saudável; todas as 20 migrações foram aplicadas e listadas localmente. O lint SQL não encontrou erros e o teste de integração passou. A branch não está ligada ao projeto e não houve escrita remota. O advisor sinalizou RLS desativado em duas tabelas privadas; foi acrescentada uma migração local para ativar RLS e reforçar os privilégios, sem a aplicar em produção. Detalhes em shared-workspace.md.

Revalidação após reconciliação (23/09/2026): `npm run check` passou; `npm test` passou com 293 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` passou com 83/83. Na stack isolada de auditoria, as 20 migrações aparecem aplicadas, `db lint --local --schema public,private --fail-on error` passou sem erros e `npm run test:supabase-local` passou a integração RLS/sincronização com duas sessões, incluindo criação/listagem/consulta/revogação do conector com RLS ativo, e o E2E de duas PWAs offline/religação. O runner aceita `VISION_COACH_SUPABASE_LOCAL_WORKDIR` para selecionar uma stack isolada; a stack normal, com histórico antigo, permaneceu intocada. Os originais aprovados continuam inalterados. Nenhuma migração foi aplicada ao projeto publicado.

UX do perfil da equipa (23/09/2026): removida a mensagem fixa e desatualizada que afirmava que a ligação do agente não estava ativa. O perfil explica agora que uma IA autorizada pode usar o MCP, liga às Definições e reafirma que ações do treinador exigem confirmação. Cache PWA v110. Validação: `npm run check`; `npm test` com 293 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` com 84/84, incluindo o novo teste a 390 px. Os originais aprovados mantêm bytes, resolução e associação.

Auditoria do gateway MCP (23/09/2026): a escrita genérica do gateway foi restringida a `game_model`; o contrato publicado pela RPC `head_coach_capabilities` anuncia o mesmo limite. A migração `20260923145609` consta aplicada na stack Supabase local isolada. `npm run check`, `npm test` (309 aprovados, 1 ignorado), `npm run test:e2e` (95/95) e `npm run test:supabase-local` (RLS/sincronização com duas sessões e PWA offline/reconexão) passaram; `git diff --check` passou. A stack local permanece parada com volumes preservados. Os 22 testes de gateway MCP/relatórios passaram. Para contornar a falha de montagem Docker do CLI 2.117.0, o CLI 2.118.0-beta.37 carregou a função e iniciou o runtime; as chamadas HTTP de verificação foram rejeitadas pelo runtime com `UNAUTHORIZED_INVALID_JWT_FORMAT`, antes dos handlers, apesar de usarem a chave sintética local `service_role`. A verificação HTTP permanece inconclusiva, sem escrita. A cópia temporária da função foi removida; volumes da stack isolada preservados. Nenhuma publicação ou escrita foi feita em produção; validação do runtime/consumidor MCP e revisão antes do deploy continuam pendentes.

Revisão integrada 23/09/2026: o detalhe do jogo agora usa o alinhamento visual como editor único; propostas pós-jogo do Head Coach mostram hipóteses e eventos citados, permitem copiar prioridade para edição e exigem revisão das fontes, decisão e confirmação antes de aceitar. A aceitação não cria treino nem atualiza memória; eventos inválidos ou com IDs duplicados não entram nas estatísticas. Auditoria do PDF/ficha longitudinal não encontrou incoerências adicionais. Cache PWA alinhado em v114. Validação final: `npm run check`; `npm test` com 310 aprovados, 1 ignorado, 0 falhas; `npm run test:e2e` 98/98; `npm run test:supabase-local` passou RLS/sync com duas sessões e PWA offline/reconexão; focados do alinhamento 15/15, análise 6/6, relatórios 13/13; `git diff --check` sem erros (avisos de conversão LF/CRLF). A chamada HTTP ao runtime Edge continua inconclusiva por `UNAUTHORIZED_INVALID_JWT_FORMAT`; aparelhos físicos, impressora, chamada live Jev e validação do consumidor MCP externo continuam pendentes. Sem publicação ou escrita em produção; dados e originais aprovados preservados.

Workspace 23/09/2026: propostas atuais de análise de jogo do Head Coach entraram na fila “Propostas por rever”, deduplicadas pelo UUID do jogo e ligadas diretamente à secção “Depois”. Propostas desatualizadas aparecem separadas, com origem navegável, e não são contadas como aprováveis. Testes cobrem duplicados, data/adversário, navegação e estado stale; E2E focados 3/3, unitários 4/4; `npm run check`, `npm test` (311 aprovados, 1 ignorado), `npm run test:e2e` (99/99) e `git diff --check` passaram. O TXT fica reservado para atualização no encerramento integral do desenvolvimento.

Arranque da PWA (23/09/2026): as cinco imagens aprovadas somam 8,98 MB e deixaram de bloquear a ativação do service worker. São guardadas sob procura numa cache estável; a ativação migra as cópias que já existiam antes de remover a cache antiga. As imagens continuam disponíveis offline depois da primeira consulta e mantêm bytes/resolução aprovados. Cache PWA v115. Validação: `npm run check`; `npm test` (312 aprovados, 1 ignorado); `npm run test:e2e` (100/100); testes focados da atualização do service worker 35/35; o E2E confirmou os cinco originais online/offline e a ativação sem downloads de imagem. Sem alteração aos ficheiros de imagem ou ao projeto Supabase publicado.

Correção do Workspace (23/09/2026): o primeiro desenho agora usa os dados locais sem aguardar a ronda remota. A sincronização corre em segundo plano; o evento de conclusão atualiza a rota sem iniciar outra ronda, eliminando o ciclo de sync→render→sync que podia tornar a app lenta. `setView()` só reinicia o scroll quando muda a rota. Coberto por regressão Playwright mobile com sync pendente, contagem de chamadas e scroll preservado. Cache PWA v116. Ainda local, não publicado.

Identidade visual Sub-8 Figueiró (23/09/2026): a paleta de interface passou a vermelho, branco e preto; o campo visual e os diagramas SVG usam vermelho escuro com marcações brancas. O emblema fornecido foi copiado sem transformação para `assets/teams/14529_imgbank.png`, com SHA-256 `3B0A2AB680D4D46E1C0864C7D9ACACF673FB4C07429473209FA1B2FC0E608194`; aparece no Workspace/perfil e no relatório de equipa quando a identidade corresponde a Figueiró + Sub-8. A nova cache PWA v117 inclui o emblema. Os PNGs aprovados dos exercícios não foram alterados. Ainda local, não publicado.

Revisão local do jogo e de conflitos (23/09/2026): as substituições confirmadas aparecem também em ordem cronológica na análise de lances e na estatística contada a partir do registo de utilização. A resolução de conflitos concorrentes permite comparar as duas versões e exige escolha explícita com revalidação de ambas; a prévia oculta conteúdo `data:` e URLs assinados. Cobertura unitária, Playwright e sanitização acrescentadas; ainda local, não publicada.

Sync 23/09/2026: reabertura read-only de Definições confirmou 11 ocorrências no PC publicado (uma eliminação concorrente de exercício e dez `activity_items`), embora o treinador tivesse contado nove. Encontrada uma falha reproduzível: atividades de criação do arquivo de épocas podiam guardar `season-index:default` como relação, quando a sincronização exige uma UUID. A app passa agora a guardar a UUID do documento; atividades antigas só convertem essa chave externa quando existe uma correspondência única, da mesma equipa e validada no remoto. Também compara timestamps imutáveis como instantes UTC, preservando conflitos de conteúdo/instante reais. Os 10 registos atuais ainda precisam de nova sync e confirmação da origem para provar quantos correspondem a esta falha. Cache PWA v118; regressões unitária e de navegador.

Validação após esta correção: `npm run check`; `npm test` com 315 aprovados, 1 ignorado e 0 falhas; `npm run test:e2e` 103/103; `npm run test:supabase-local` na stack Supabase isolada com RLS/duas sessões e PWA offline-religação passou. `git diff --check` passou, com avisos de conversão LF/CRLF do Git. As 11 ocorrências remotas foram apenas lidas; sem sincronização, consolidação ou resolução em produção.

Auditoria local MCP/Edge (23/09/2026): no stack Supabase isolado, head-coach-gateway respondeu HTTP 200 a capabilities com JWT local; vision-coach-mcp respondeu HTTP 200 a initialize com um conector local temporário, versão vision-coach 1.12.0. O primeiro 401 observado vinha da configuração incompleta da stack temporária (verify_jwt não estava desativado para MCP); a configuração do repositório já define verify_jwt = false para esse endpoint e o código valida o conector próprio. A credencial temporária foi revogada e a listagem confirmou enabled=false e revoked_at preenchido; runtime de teste encerrado. Isto valida o runtime isolado, não a integração publicada nem consumidor MCP externo. Sem alteração ou publicação em produção.

Sync das atividades pendentes (23/09/2026): após v118, a revisão da fila publicou uma causa mais específica para dez atividades locais: UUID de origem não presente na equipa ou origem local já inexistente. O evento é preservado e sincronizado sem vínculo inválido, mantendo a referência e o motivo em metadados visíveis na Timeline; origens ambíguas, de outra equipa ou sem equipa confirmada continuam bloqueadas. Cache PWA v119.

Validação pública v119 (23/09/2026): PR #47 integrado em `main` como `bb36d64`. `npm run check`, `npm test` (318 aprovados, 1 ignorado), `npm run test:e2e` (104/104) e o teste Supabase local de duas sessões passaram antes da publicação. Na abertura inicial de Definições, os dez conflitos ainda apareciam; após a sincronização automática terminar, a mesma instalação mostrou zero conflitos no Workspace e em Definições. Plantel (6), plano (1), media (4) e atividade (50) continuavam presentes. Nenhum registo foi resolvido ou apagado manualmente. A publicação de `activity_log` em Realtime na base de produção continua pendente de revisão e migração.

Auditoria adicional de Realtime local: a publicação Supabase local contém `public.activity_log` e um cliente de teste com chave de serviço recebeu `SUBSCRIBED`. A tentativa anterior de testar a entrega autenticada evento→segundo treinador falhou sem provar a entrega real. Corrigi a regressão de teste para usar limites separados na confirmação da subscrição e espera do evento, incluir estados/erros do canal na mensagem de timeout e limitar a remoção do canal/socket para que a limpeza das contas/equipa sintéticas continue. A entrega real permanece por revalidar: nesta revisão, Docker devolveu HTTP 500 no endpoint do engine e a suíte de integração ficou ignorada por falta da stack local. Nada foi escrito no projeto Supabase de produção.

Estado Realtime da PWA (23/09/2026): a interface agora diferencia ligação a iniciar, ligada e degradada/fechada, mostra aviso em Definições e atualiza apenas os indicadores sem redesenhar a rota. Isto comunica o estado do canal, não confirma a inscrição de cada tabela; a inclusão de `activity_log` no Realtime de produção continua pendente de migração e revisão, e não foi alterada. Cache PWA v120. Validação local: `npm run check`; `npm test` (318 aprovados, 1 ignorado); `npm run test:e2e` (105/105); teste Playwright específico do aviso (1/1); `git diff --check`. Sem publicação nem alterações aos dados/imagens aprovadas.

Confirmação de inscrição Realtime (23/09/2026): `SUBSCRIBED` podia significar apenas que o canal entrou, antes de o servidor confirmar as inscrições Postgres. `RemoteWorkspace.startRealtime()` agora usa `postgres_changes_options.wait: true`, pelo que o aviso só indica Realtime ativo depois da confirmação das tabelas; falhas de publicação passam para estado degradado. Baseado na API do Supabase JS 2.116 e documentado localmente; sem escrita no Supabase de produção. Cache PWA v121. Validação: `npm run check`; `npm test` (318 aprovados, 1 ignorado); Playwright (105/105). O E2E encontrou uma corrida preexistente no setup do jogo visual: passou a esperar também `RemoteWorkspace`; teste mobile/desktop focado 2/2 e suite completa 105/105.

Realtime isolado por escopo (23/09/2026): subscrições das tabelas principais e de `activity_log` passam a canais separados, para uma falha da tabela de atividade não degradar eventos de plantel, sessões, media ou equipa. O estado agregado só indica ligação completa depois da confirmação de ambos; alterações do canal principal continuam a iniciar sincronização mesmo se atividade estiver degradada. Cache PWA v122. Validação local: `npm run check`, `npm test` (318 aprovados, 1 ignorado), Playwright completo (105/105), teste focado de sessão desktop/mobile (2/2) e `git diff --check`. Sem migração Supabase ou alteração de dados e imagens aprovadas.

Combinação assistida de conflitos e correção do scroll durante sync (24/09/2026): gravações remotas guardam a última versão comum como metadado local e excluem-na do payload sincronizado. Na comparação do treinador, a app propõe combinar apenas alterações em campos diferentes, mostra a prévia e pede confirmação; a escrita volta a validar as versões remota e local. Alterações sobrepostas, media, atividade imutável, referências por resolver e registos antigos sem base continuam a exigir escolha manual. No fim da sync, o Workspace só restaura o scroll antigo se o treinador não tiver rolado nem mudado de rota durante o redesenho. Cache PWA v123. Validação local: `npm run check`; `npm test` com 318 aprovados, 1 ignorado; E2E focado de conflitos/scroll 3/3; suite geral com `--retries=1`: 106 passaram e 1 teste intermitente de carregamento passou na repetição. Uma execução integral anterior também teve um timeout isolado à espera de script; o teste passou isolado. Sem publicação, escrita Supabase ou alteração de dados/imagens aprovadas.

Resolução manual de conflitos legados (24/09/2026): para registos sem `_sync_base` ou com campos sobrepostos, a comparação agora lista campos que diferem e permite escolher explicitamente PC ou workspace remoto em cada um. Campos iguais permanecem e campos removidos continuam removidos só quando essa versão é escolhida. O cliente recusa escolhas incompletas/extra e volta a validar as revisões antes de gravar; media e atividade imutável continuam fora da combinação. Cache PWA v124. Validação local: regressão de dois dispositivos 1/1 (inclui campo removido); `npm run check`; `npm test` com 319 aprovados, 1 ignorado; três E2E focados passaram; suite Playwright completa, 107 passaram e 1 teste intermitente de evidência de vídeo passou na repetição; `git diff --check`. Nenhuma migração, escrita Supabase ou publicação. Dados e imagens aprovadas preservados.

Carregamento paralelo do Workspace (24/09/2026): sessão remota, snapshot local e documentos da época deixaram de ser consultados em série. O E2E controla as três respostas e prova que começam em paralelo antes de libertar qualquer uma. Cache PWA v125. Validação local: `npm run check`; `npm test` 319 aprovados, 1 ignorado; E2E focado do carregamento 1/1; suite Playwright completa, 108 passaram e 1 teste intermitente de sincronização de Planos passou na repetição; `git diff --check`. Sem migração, escrita Supabase, mudança de dados/imagens ou publicação.

Carregamento local-first dos Planos (24/09/2026): a lista abre logo com documentos locais enquanto a sincronização remota decorre; o evento de sync-complete atualiza a lista sem bloquear navegação. Cache PWA v126. E2E dedicado prova primeiro a renderização local e depois a chegada do documento remoto. Sem alterações a dados, Supabase ou imagens.

Validação Realtime local (24/09/2026): a stack Supabase isolada recebeu migration, lint sem erros, teste RLS/round-trip/conflito/tombstone/Storage com duas sessões (1/1), evento activity_log por Postgres Changes entre treinadores com isolamento de terceiro utilizador (1/1) e E2E de duas PWA offline/reconexão/conflito/não ressurreição (1/1). Foi corrigida a corrida do próprio teste aguardando SUBSCRIBED antes de escrever. Stack encerrada; produção e dispositivos físicos não foram tocados.

RAG do conhecimento da equipa (24/09/2026): adicionada pesquisa híbrida pgvector + lexical no schema privado, chunks com proveniência/revisão, fila idempotente via trigger, remoção de tombstones, filtros por equipa/jogo/treino/jogador/data/categoria e ferramentas MCP de pesquisa/reindexação. A indexação é allowlist; disponibilidade, dados médicos, perfis e media ficam fora. A consulta de últimos jogos ordena datas válidas antes dos registos sem data; `match_refs` filtra até dez UUIDs únicos numa chamada e mantém isolamento por equipa. Migração e overload RPC testados numa stack isolada: dois jogos da equipa foram recuperados e um jogo alheio foi excluído. `db lint` não encontrou erros; `test:supabase-local` passou (3 integrações + E2E offline/reconexão); o HTTP MCP local passou com pesquisa sem provider, schema `match_refs` e rejeição de token não autorizado (1/1). `npm run check`, `npm test` (334 passaram, 4 integrações locais ignoradas) e `git diff --check` passaram. A migration/Edge Function ainda não foram publicadas; nenhum segredo foi consultado/configurado e o provider semântico aguarda configuração autorizada. Sem alteração de produção, dados do treinador ou imagens aprovadas.

Atualização de proveniência RAG (24/09/2026): alterações a `teams.metadata.escalao`/`age_group` reenfileiram fontes ativas da mesma equipa; metadados não relacionados não provocam reindexação. Integração local confirmou que chunks existentes passam de Sub-8 para Sub-9 na pesquisa seguinte, sem criar registos duplicados. `db lint` passou; teste RAG Supabase 1/1. A primeira execução da suíte Supabase falhou uma vez no E2E offline com `mediaSubjectKey is not defined`; o mesmo E2E isolado e a suíte completa repetida passaram sem alteração na app. Stack descartável encerrada e credenciais locais limpas.

Regressão integrada da app (24/09/2026): scroll/Workspace e conflitos focados passaram 7/7; a execução completa Playwright passou 109/109 com `--retries=1`. Dois testes que falharam sob carga na primeira corrida passaram ambos repetidos isoladamente; não foi reproduzido defeito funcional. Nenhuma alteração de comportamento foi feita com base apenas na corrida instável.

Correção de relatórios (24/09/2026): treinos legados sem snapshot resolvem imagem privada pelo registo do exercício; se não carregada, o PDF mostra estado em vez de omitir silenciosamente. Ficha de jogo identifica indisponibilidade como estado atual do plantel, sem a sugerir como histórico do dia do jogo. Regressões E2E para imagem disponível/indisponível e rótulo temporal. Validação: npm run check, npm test, E2E geral 112/112, E2E de relatórios 15/15 e git diff --check; nenhuma imagem aprovada foi modificada. Docker local continua sem responder; sem publicação.

Correção do Workspace e evolução individual (24/09/2026): filas de aprovação/propostas ignoram documentos arquivados; cronologia do atleta inclui evidências do objetivo datadas e ligadas às origens, e distingue exercícios associados de exercício realizado. Cache PWA v127. Validação: npm run check; npm test (340 aprovados, 4 integrações locais ignoradas); Playwright geral 112/112; git diff --check. Sem alteração de dados ou publicação.

Regressões do módulo de jogo (24/09/2026): lances criados com o minuto predefinido passam a usar o cronómetro no instante de gravação; um minuto ajustado manualmente mantém-se, assim como o minuto de lances editados. Evidências de vídeo ligadas a um problema continuam legíveis e editáveis se a análise desse problema for limpa depois; a app identifica a ligação como histórica sem impedir abertura do jogo ou do relatório. Cache PWA v128. Validação: npm run check; npm test (341 passaram, 4 integrações Supabase locais ignoradas); Playwright focado 11/11, regressão do service worker 2/2 e suíte geral 114/114; git diff --check. Nenhum registo de treinador ou imagem aprovada foi alterado; sem publicação.

Recuperação RAG multi-jogo (24/09/2026): quando há limite por jogo explícito, o RPC agora recolhe até 48 candidatos semânticos e 48 lexicais por jogo antes da fusão, para que chunks numerosos de um só jogo não eliminem os restantes candidatos. Mantém filtro de equipa/origem ativa, ordem por relevância e máximo final por jogo. Validação atual: `npm run check`; `npm test` (341 passaram, 4 integrações locais ignoradas); testes RAG/schema 28/28; `git diff --check`. A execução Postgres da migration ainda está pendente: `docker info` não devolveu resposta; nenhuma migration ou dado foi enviado a produção.

Contexto híbrido dos últimos jogos (24/09/2026): nova operação MCP read-only `get_recent_match_context` seleciona até cinco jogos concluídos datados da equipa autorizada, devolve resultados e contagens registadas de eventos/perdas por motivo e zona, e junta excertos RAG citados com limite por jogo. Eventos e motivos não registados permanecem explicitamente ausentes; não se calcula causalidade. Testes cobrem janela temporal, exclusão de jogos futuros/sem data, contagens, equilíbrio RAG, isolamento por scope e ausência de escrita. Perdas sem motivo permanecem classificadas como “sem motivo”; apenas motivo `other` explícito é apresentado como “outro”. Validação atual: `npm run check`; `npm test` (350 passaram, 4 integrações locais ignoradas); RAG dedicado 18/18; migration executada em PostgreSQL WASM/pgvector; fluxo HTTP Edge e `db lint` locais ignorados por stack local indisponível. Sem publicação ou escrita remota.

Seleção temporal dos jogos recentes (24/09/2026): a query Supabase aplica o limite até ao dia atual antes de ordenar e limitar a 100 registos, evitando que jogos concluídos com datas futuras ocultem jogos recentes válidos. A regressão verifica que o limite temporal está na query, bem como ordenação, seleção dos cinco jogos e estatísticas ausentes. `npm run check`, `npm test` e `git diff --check` repetidos nesta alteração; sem escrita remota ou publicação.

Pesquisa mais rápida e navegação de origem (24/09/2026): a página de pesquisa já não constrói o snapshot/timeline completo do Workspace nem faz leituras de dados em série; carrega em paralelo as oito fontes de que necessita. A regressão de desempenho verifica sobreposição das leituras e ausência do snapshot. O E2E também revelou referências antigas que impediam navegar de uma observação ao jogo/treino associado; corrigidas para usar os conjuntos carregados pela pesquisa. Cache PWA v129 alinhada em `sw.js`, `index.html` e testes. Validação final: os cinco E2E focados passaram; suíte Playwright geral 115/115; `npm run check`; `npm test` (342 passaram, 4 integrações locais ignoradas); `git diff --check`.

Proveniência de exercícios no índice RAG (24/09/2026): os chunks dos blocos de sessão mantêm em `metadata.related_refs` a UUID remota do exercício usado; identificadores locais não passam para o índice. Teste unitário confirma UUID estável e rejeição de referência numérica local. Ainda local; não altera o plano original nem a biblioteca de exercícios.

Fila de avaliações no Workspace (24/09/2026): treinos concluídos podem guardar a avaliação em `review` do plano ou em `session.review`. O painel consulta ambos, seguindo a precedência do plano, para não voltar a apresentar como pendente uma sessão já avaliada. Regressão cobre revisão concluída/pendente nos dois formatos e treino legado sem avaliação. Cache PWA v130 alinhada; E2E focado e imagens do service worker passaram 1/1 cada, `npm run check` passou, `npm test` 343 aprovados/4 integrações locais ignoradas e Playwright geral 116/116. `git diff --check` passou. Continua sem publicação.

Auditoria da reconciliação PC↔telemóvel (24/09/2026): confirmei que já há versão-base local, combinação com confirmação apenas para campos não sobrepostos e seleção por campo para conflitos sobrepostos/legados; a ação recusa revisões alteradas desde a comparação. Acrescentei verificação mobile de 390 px ao fluxo legado. `tests/remote_workspace.test.js` passou 79/79, E2E focado em conflitos 6/6, `npm run check` e `git diff --check` passaram. Corrigi a documentação partilhada que ainda dizia que combinação campo a campo não existia. A integração com Supabase real/duas sessões permanece pendente enquanto Docker não responde; sem alteração de dados nem publicação.

Desempenho do Workspace (24/09/2026): o painel passou a carregar os documentos ativos/arquivados numa única leitura integrada no snapshot, contar media pelo índice sem clonar os respetivos `data_url`, e omitir a timeline que não é exibida. `HeadCoachMemory.list()` agora consulta a store pelo índice `team_id` em vez de varrer todos os registos. O contrato padrão do snapshot para o agente mantém timeline e media completos; as otimizações só se aplicam ao painel. Cache PWA v131. Validação: `npm run check`; `npm test` 343 aprovados/4 integrações locais ignoradas; Playwright 116 testes passaram e um ficou marcado como flaky após timeout no primeiro arranque do teste de conflito, que passou na repetição; os dois E2E de leitura otimizada passaram; `git diff --check`. Sem publicação nem alterações a dados/imagens aprovadas.

Instruções RAG na inicialização MCP (24/09/2026): acrescentei ao `initialize` a seleção entre contexto de treino, últimos jogos e pesquisa textual genérica; as instruções exigem fontes citadas, distinção facto/observação/interpretação/hipótese, declaração de evidência insuficiente e confirmação explícita para executar decisões do treinador. Versão da Edge Function local `1.14.0`. Testes MCP/RAG dirigidos 36/36, `npm run check`, `npm test` 343 aprovados/4 integrações locais ignoradas. Sem publicação; Docker continua indisponível para validação Postgres local das migrations.

Validação reprodutível das migrations RAG (24/09/2026): adicionei PGlite e a extensão pgvector apenas como dependências de desenvolvimento e incluí `tests/team_knowledge_migrations.pglite.test.js` na suíte Node. O teste executa as três migrations em Postgres WASM com dados fictícios e cobre fila/claim, gravação de chunks, pesquisa multi-jogo equilibrada, scope entre equipas, privilégios, RLS ativado e remoção soft/física; teste dirigido passou. É validação de SQL/Postgres, não substitui Auth/PostgREST/Edge nem a stack Supabase descartável. Repetir o lint e a integração Supabase/Edge continua pendente porque o Docker Desktop devolve HTTP 500. Nenhuma publicação, secret ou dado remoto alterado.

Proveniência de estatísticas RAG recentes (24/09/2026): `get_recent_match_context` passa a devolver `null` para contagens, quebras por motivo/zona e número de lances quando `match_events.events` não está disponível; uma lista presente mas vazia permanece zero contado. `missing_data.structured_event_counts` sinaliza dados parciais se pelo menos um jogo da janela não tem lances. Regressão MCP cobre jogos com dados, sem campo e lista vazia. Testes RAG 17/17 passaram; `npm run check`, `npm test` (344 passaram, 4 integrações Supabase ignoradas) e `git diff --check` passaram. Sem publicação ou escrita Supabase.

Robustez do teste Realtime de atividade (24/09/2026): separei o timeout de handshake do timeout de entrega do `activity_log`, incluí os estados/erros do canal no diagnóstico e limitei a espera de remoção de canal/socket para continuar a limpar fixtures sintéticas. `node --check tests/realtime_supabase_local.test.js` e `npm run check` passaram. O teste de integração foi ignorado sem as chaves/URL locais; a tentativa atual de consultar Docker terminou em HTTP 500, portanto a entrega entre sessões autenticadas continua sem confirmação. Sem publicação ou escrita remota.

Consolidação de conflitos e fecho da revisão RAG (24/09/2026): removi entradas repetidas na lista de conflitos quando o mesmo registo reaparece em fases diferentes da sincronização/consolidação, mantendo conflitos com causas distintas e sem descartar trabalho. Excertos RAG são explicitamente tratados como dados não confiáveis, nunca como instruções. Corrigi a expectativa de cache da PWA para v132. Validação final: `npm run check`; `npm test` com 345 aprovados, 0 falhas e 4 integrações Supabase locais ignoradas; Playwright focado 71/71; testes de sincronização 80/80; `git diff --check`. O funcionamento Supabase/Edge real, provider de embeddings, sincronização autenticada entre dispositivos e publicação permanecem pendentes; não foi escrita informação remota nem alterada imagem aprovada.

Atalho da fila de conflitos no Workspace (24/09/2026): `Rever conflitos` abre as Definições já posicionadas na lista, reduzindo a navegação até às decisões pendentes. Teste E2E valida a posição do destino em viewport móvel; `npm run check`, `npm test` (348 aprovados, 4 integrações locais ignoradas) e o teste E2E dirigido passaram. Na suíte completa, 121/122 passaram; o caso existente de navegação ao perfil da equipa falhou uma vez e passou isoladamente. Cache PWA v137. Alterações locais para revisão.

Correção de salto do Workspace (24/09/2026): após renderização da mesma rota, a restauração do scroll agora só acontece se a posição real continuar igual à capturada. Isto cobre mudanças pela barra de scroll entre o render e o `requestAnimationFrame`, que não disparam eventos `wheel` ou `touchmove`. Regressão E2E específica, scroll mobile/desktop, atalho de conflitos e atualização do service worker passaram 8/8; `npm run check`, `npm test` (350 aprovados, 4 integrações locais ignoradas) e `git diff --check` passaram. Cache PWA v138. Sem publicação.

Desempenho das Definições e revisão de conflitos (24/09/2026): a página deixa de carregar blobs de media para mostrar a contagem, omite a timeline não exibida, executa snapshot e estado remoto em paralelo e consulta lista de equipas/conectores MCP simultaneamente. Regressão verifica que não há leitura de media, que se conta pelo índice e que as consultas independentes começam juntas. E2E focado passou; `npm run check`; `npm test` (350 aprovados, 4 integrações locais ignoradas); Playwright geral 127/127; `git diff --check`. Cache PWA mantém v138. Sem publicação.

Nova otimização e regressão do Workspace (24/09/2026): sincronizações que apenas enviam registos ou repetem conflitos iguais já não reconstruem a página. O painel atualiza após pulls, eliminações ou mudanças reais no conjunto de conflitos; uma falha recuperada limpa o aviso sem nova leitura. E2E confirma posição de scroll, pull, novo conflito e repetição idempotente. `npm run check` e `npm test` (352 aprovados, 4 integrações Supabase locais ignoradas) passaram; Playwright completo 129/129. Cache PWA v140. Permanece local, sem publicação.

Latência da pesquisa RAG sem fila pendente (24/09/2026): o MCP consulta agora a fila idempotente antes de ler metadados auxiliares; quando não há indexação por fazer, salta as queries de equipa e plantel que só servem para preparar chunks, sem alterar resultados nem escopo da pesquisa. Se essas leituras falharem após reclamar trabalhos, liberta as leases para não atrasar novas tentativas. Teste RAG novo cobre a libertação imediata e as consultas desnecessárias evitadas. A versão local do servidor MCP passa a `1.14.1`. Cobertura focada MCP/RAG/Postgres WASM 39/39; `npm run check`; `npm test` 353 aprovados, 4 integrações locais ignoradas. O fluxo Edge HTTP em Supabase local continua por validar sem a stack Docker; sem acesso a produção.

Privacidade RAG para atletas apagados (24/09/2026): a lista temporária de ocultação percorre por páginas o histórico de nomes de atletas da equipa, incluindo registos soft-deleted, enquanto a fila continua a excluir as fichas eliminadas. Isto impede que uma nota histórica retida reenvie o nome ao provider quando o jogo/treino é reindexado, mesmo após mais de 150 atletas registados. Regressões verificam que o nome não aparece no chunk nem no pedido de embedding. `npm run check`, teste RAG 21/21, `npm test` 355 passaram/4 integrações locais ignoradas e `git diff --check` passaram. Versão MCP local `1.14.2`; sem publicação nem dados reais enviados.

Diagnóstico da stack local (24/09/2026): iniciar Docker Desktop não recuperou o engine; `/info` responde HTTP 500 e o log do backend repete `no route to host` para `192.168.65.7:2376`. O distro `docker-desktop` aparece como `Running`, mas comandos dentro dele não concluem; Ubuntu está parado. Não reiniciei WSL nem removi containers/volumes. Continuam pendentes `db lint` e as integrações locais Auth/PostgREST/Edge; os testes unitários e PostgreSQL WASM mantêm-se verdes.

Auditoria automatizada de sincronização (24/09/2026): `tests/remote_workspace.test.js` passou 81/81; Workspace e UX móvel no navegador passaram 52/52. A cobertura inclui offline/religação, eliminação sem ressurreição, edição simultânea com conflito explícito, conciliação em lote só para campos independentes, fotos em Storage privado, jogos/lances/minutos e propostas/memória, além da retenção do scroll. Estes testes usam bases/browser simulados; sincronização real PC↔telemóvel e verificação física continuam pendentes.

Auditoria de jogo, evidências e relatórios (24/09/2026): testes de modelo/MCP para análise, lances, vídeo, quadro visual e relatórios passaram 90/90. Os E2E focados em análise, acontecimentos e PDF passaram 29/29. A cobertura confirma separação facto/observação/interpretação/hipótese/decisão, dados ausentes sem fabricação, revisão/confirmação do treinador, timestamps editáveis/apagáveis, ligações a fontes persistidas e PDFs imprimíveis com origem e histórico. TypeSafe real e a publicação do MCP continuam por validar no runtime; estes testes usam integrações simuladas.

Auditoria de época e evolução (24/09/2026): uma execução inicial isolada encontrou falha de sincronização no próprio teste: esperava `TeamDevelopment` e acedia a `RemoteWorkspace` antes da ordem normal de scripts terminar. Corrigi a condição do teste para aguardar ambos os módulos; não alterei comportamento da app. A suíte E2E focada completa passou 10/10 após a correção; `npm test` passou com 355 aprovados, 0 falhas e 4 integrações Supabase locais ignoradas; `npm run check` passou. A auditoria cobre época/período, objetivos individuais, evolução de equipa, avaliação explícita e proposta do Head Coach.

Auditoria de calendário e treino (24/09/2026): calendário, planeador, sessão em campo e continuidade passaram 56/56 testes de modelo/MCP e 15/15 E2E. Verificados duplicação/reordenação, presença explícita, cronómetro com reload/offline, notas por bloco, conflitos de revisão, cópias sem execução histórica e aprovação humana da proposta seguinte. Sem defeito de produto reproduzido.

Auditoria de UX/PWA e imagens aprovadas (24/09/2026): controlos mobile, calendário sem duplicação/overflow, scroll durante sync, anúncio acessível do aviso PWA e cache offline das imagens passaram 5/5 E2E; testes de imagens aprovadas passaram 6/6. O workflow verifica hash/bytes originais e não recompõe as imagens.

Revisão das escritas MCP e validação geral (24/09/2026): confirmei confirmação explícita, scope, equipa e revisão atual para alterações de disponibilidade, época, objetivos, treino, análise, jogo, presenças, memória, relatórios e imagens. Fechei uma lacuna no registo MCP de media externa: o handler agora valida também os tipos de origem e media e limita título, nota e URL antes do RPC, mesmo se o cliente não validar o schema MCP. `npm run check` passou; testes MCP/imagens 122/122; `npm test` 355 passaram, 0 falhas e 4 integrações locais ignoradas; Playwright geral 129/129. `docker info` permaneceu sem resposta por 15 s e foi interrompido; validação Auth/PostgREST/Edge numa stack local e PC↔telemóvel físico continuam pendentes. Nenhuma publicação, alteração remota, dado do treinador ou imagem aprovada foi feita.

Integrações locais ignoradas e runner (24/09/2026): identifiquei os quatro testes em `remote_workspace_supabase.local.test.js`, `realtime_supabase_local.test.js`, `team_knowledge_supabase_local.test.js` e `team_knowledge_edge_http.local.test.js`; três precisam de Supabase local + URL/chaves temporárias geradas pela CLI, e o HTTP também precisa da URL local da Edge Function MCP. A CLI 2.117.0 está disponível por `npx`, mas `status` não responde por causa do engine Docker; limitei a espera do runner a 15 s com erro específico e teste de regressão. `node --test tests/supabase_local_runner.test.js` e `npm run check` passaram. `npm run test:supabase-local` agora falha em 15 s com o diagnóstico correto; não chegou a criar utilizadores, equipas ou dados.

Validação Edge/TypeSafe independente do Docker (24/09/2026): `deno check --node-modules-dir=auto --no-lock supabase/functions/vision-coach-mcp/index.ts` passou, sem alterar manifests; a suíte Node voltou a resolver Playwright 1.62.0 igual ao lockfile e passou 356/356 ativos (4 integrações locais ignoradas). A documentação TypeSafe atual confirma endpoint `/v1/systemone`, modelo `jev-latest`, formato Noul e `answers.<id>.noul`, que correspondem ao cliente; Noul é probabilidade yes/no, sem confidence separado. A chamada externa real continua sem validação porque não há configuração local autorizada de `TYPESAFE_API_KEY`; nenhuma citação ou dado real foi enviado ao serviço.
Proveniência de estatísticas em jogos sem registo (24/09/2026): ausência de `match_events.events` já não se converte em zero na interface, análise, relatórios ou MCP; o estado é desconhecido (`null`). Uma lista explicitamente guardada e vazia continua a significar zero contado. O seletor de evidências mostra “não registada” em vez de zero para estatísticas indisponíveis. Testes de modelo/MCP/relatórios 45/45, `npm run check`, `npm test` 358 aprovados, 0 falhas e 4 integrações locais ignoradas; Playwright focado 5/5. `git diff --check` passou. Supabase local real continua por validar porque o Docker engine retorna erro; sem publicação nem escrita remota.

Cobertura explícita das estatísticas e minutos nos relatórios (24/09/2026): o resumo agregado soma apenas jogos com lista de lances persistida e indica quantos jogos têm essa origem e quantos não têm; uma lista vazia guardada é zero conhecido. O relatório individual e o MCP contam separadamente jogos/convocações sem minutos conhecidos, mesmo quando outros jogos têm cronómetro. Regressões E2E e MCP cobrem períodos mistos. `npm run check`, `npm test` (358 aprovados, 0 falhas, 4 integrações locais ignoradas) e os 18 E2E de relatórios passaram; `git diff --check` passou. A validação real Supabase continua bloqueada pelo Docker indisponível.


Invalidação de chunks RAG ao mudar o escalão (24/09/2026): a migration `20260924101056_invalidate_team_knowledge_on_age_group_change.sql` redefine o trigger para remover de imediato vetores com metadados antigos e reenfileirar fontes ativas sem duplicados. O teste PostgreSQL WASM executa agora as quatro migrations em ordem, comprova que chunks antigos deixam de ser pesquisáveis e que voltam após reindexação; `node --test tests/team_knowledge_migrations.pglite.test.js` passou. Nenhuma origem desportiva é alterada. Integração Supabase/Edge real continua pendente por indisponibilidade do Docker.

Revalidação da app e scroll (24/09/2026): `npm run check` e `npm test` passaram, com 358 testes aprovados e quatro integrações Supabase locais ignoradas. A suíte Playwright completa passou 130/130 depois de atualizar uma expectativa de texto já alterado na interface e impedir que a sync agendada no arranque fosse contada como parte do teste explícito. O teste de scroll durante sync também passou isoladamente. A CLI Playwright do `npm run test:e2e` foi desviada por um shim Deno extrínseco em `node_modules/.bin`; executar a CLI bloqueada no lockfile via Node evitou mexer nas dependências do workspace. `npm run test:supabase-local` continua a exceder o limite de 15 s a consultar o Docker Engine, por isso Auth/PostgREST/Edge local e o teste em aparelhos físicos permanecem por confirmar. Nenhuma escrita remota ou publicação foi feita.

Proteção de privacidade RAG e reconciliação leve de sincronização (24/09/2026): adicionada uma quinta migration RAG que invalida imediatamente chunks de toda a equipa quando um atleta é renomeado, transferido ou removido e reenfileira fontes ativas; PGlite verifica nome alterado, soft delete e hard delete. O indexador amplia os termos de saúde excluídos e separa objetivo/plano/definição/princípio de jogo de observação. A consolidação passou a consultar UUIDs não associados em lotes de 100 e não agenda uma sincronização redundante no arranque. Conflitos com ID local inválido podem apresentar uma correspondência externa única e só ligam após comparação e confirmação, preservando edição e versão esperada. Validação: `npm run check`, `npm test` 362 aprovados/0 falhas/4 integrações locais ignoradas, E2E completo 130/130, E2E de cache PWA 3/3, regressões RAG/PGlite e sincronização passaram; cache PWA atualizada para v141 sem alterar imagens. Docker local continua indisponível para Auth/PostgREST/Edge; Supabase remoto e deploy permanecem sem alteração até revisão.

Redução de carga do Workspace (24/09/2026): o painel operacional percorre jogos e treinos por cursor IndexedDB e calcula próximo evento, avaliações pendentes e propostas sem carregar nem manter em memória todos os payloads históricos. As páginas que precisam do histórico continuam a usar o snapshot completo; o painel preserva contagens, destinos e validação de frescura das propostas. Regressão com 40 jogos e 40 treinos, cada um com eventos/notas extensos, confirmou 80 registos percorridos, resumo correto e ausência de materialização dos históricos. Cache PWA v142. Validação: `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações Supabase locais ignoradas), E2E Workspace/mobile/service worker 54/54 e `git diff --check`. Nenhuma imagem aprovada, dado real, Supabase remoto ou publicação foi alterado.
Revalidação completa após a redução de carga do Workspace (24/09/2026): a suíte Playwright integral passou 131/131, incluindo o novo percurso de 40 jogos e 40 treinos com payloads extensos e os testes de atualização PWA v142. `npm run check`, `npm test` (363 aprovados/0 falhas/4 integrações locais ignoradas) e `git diff --check` passaram. Supabase local continua indisponível via Docker; sem alterações remotas ou publicação.

Pesquisa transversal em históricos extensos (24/09/2026): jogos e treinos passam a ser pesquisados por cursor IndexedDB; a pesquisa aplica texto, tipo, datas e época durante o percurso e conserva só os 100 resultados mais recentes. Os links de observações continuam a usar UUIDs estáveis para abrir jogos/treinos de origem. Regressão cria 120 jogos e 120 treinos com payloads extensos, impede `getAll` nesses históricos e confirma contagem/ordem. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações Supabase locais ignoradas), pesquisa E2E 5/5 e `git diff --check` passaram. Cache PWA v143. A validação Supabase local/Edge real e a sincronização em aparelhos físicos continuam pendentes; sem publicação ou escrita remota.

Revalidação navegador após pesquisa em fluxo (24/09/2026): a primeira execução completa passou 131/132, com falha isolada no arranque de um teste pré-existente de sessão de treino; o teste passou isoladamente e uma segunda execução integral passou 132/132. O E2E de pesquisa permanece 5/5. Sem publicação ou escrita remota.

Arquivo de épocas com sessões históricas (24/09/2026): a página percorre jogos e treinos por cursor e retém só os campos apresentados nos cartões (data, identidade, adversário/objetivo e resultado), descartando grandes blocos de eventos e notas após cada registo. Regressão com 40 jogos e 40 treinos, cada um com 100 itens de payload, proíbe `getAll` nesses stores e confirma contagens, links e resultados da época. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas), E2E de épocas 4/4, Playwright geral 133/133 e `git diff --check` passaram. Cache PWA v144. Sem publicação ou escrita remota.

Históricos do atleta com leitura incremental (24/09/2026): objetivos, cronologia, presenças de treino e participação em jogos percorrem IndexedDB por cursor. A ficha retém apenas metadados de jogos/treinos para as opções históricas e os registos ligados ao atleta para calcular participação e abrir as origens. Regressão com 40 jogos e 40 treinos com payloads extensos impede `getAll`, preserva as 80 opções de evidência e confirma a cronologia. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas), E2E da ficha 2/2 e Playwright integral 134/134 passaram. Cache PWA v145. Sem publicação ou escrita remota.

Evolução da equipa sem snapshots históricos completos (24/09/2026): a página já não constrói o snapshot operacional inteiro; lê apenas documentos, exercícios, memória e projeções leves de jogos/treinos por cursor. Mantém todas as opções de associação e permite escolher sessões concluídas com base nos estados gravados. Regressão com 50 jogos e 50 treinos e payloads extensos impede `getAll`, verifica as 100 opções de sessão e confirma que notas/eventos não entram na vista. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas), E2E da evolução 5/5, Playwright integral 135/135 e `git diff --check` passaram. Cache PWA v146. Sem publicação nem escrita remota.

Agenda sem carregamento do histórico completo (24/09/2026): a página de calendário lê equipa, jogos e treinos em cursores IndexedDB; só conserva eventos dentro das seis semanas apresentadas, mantendo o horário recorrente e a deduplicação de treinos reais. Regressão com registos anteriores, no horizonte e posteriores proíbe `getAll`, verifica datas, metadados e ausência de duplicação. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas), E2E focado 1/1, Playwright integral repetido 136/136 e `git diff --check` passaram. A primeira corrida integral teve um timeout de arranque isolado (135/136); o caso passou isolado e a segunda suíte completa passou. Cache PWA v147. Sem publicação, escrita remota ou alteração de imagens aprovadas.

Perfil e plantel sem snapshot operacional (24/09/2026): a página Equipa carrega o perfil diretamente e consulta apenas atletas e fotografias, sem montar memória, documentos, jogos ou treinos. E2E verifica perfil/plantel e falha se a página invocar `buildSnapshot` ou ler históricos. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas), E2E focado 1/1 e Playwright integral 137/137 passaram. Cache PWA v148. Sem publicação, escrita remota ou alteração de imagens aprovadas.

Timeline por cursor e projeção (24/09/2026): a página combina cursores de atividade, documentos, memória, jogos e treinos, conserva só os 100 eventos mais recentes (máximo de 50 atividades), exclui documentos/memórias arquivados e mantém as referências usadas para abrir a origem. E2E cria 120 registos por tipo, proíbe `getAll`, verifica limite, ordem, links e projeções sem payload extenso. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas) e E2E focado 1/1 passaram. O diagnóstico mostrou `ERR_CONNECTION_REFUSED` ao carregar um script com o servidor simples de teste; substituí o servidor Python sequencial por `scripts/test-server.mjs`, que atende pedidos concorrentes. O teste de conflito semanal passou a esperar a renderização do router e confirma ID/data antes da segunda edição; repetição focada 5/5. Playwright integral final passou 138/138. Cache PWA v149. Sem publicação, escrita remota ou alteração de imagens aprovadas.

Contadores de Definições sem snapshot (24/09/2026): jogadores ativos, documentos não arquivados, atividade visível (limite de 50) percorrem cursores; media usa contagem IndexedDB sem ler bytes. A tela não monta memórias nem históricos de jogo/treino. Regressão valida 2 jogadores ativos, 2 documentos, 3 media e 50 de 55 atividades, e proíbe snapshot/leituras históricas. O teste de contrato existente foi atualizado para validar o novo fluxo e a execução em paralelo com o estado remoto. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas), E2E focado 1/1 e Playwright integral 139/139 passaram. Cache PWA v150. Sem publicação, escrita remota ou alteração de imagens aprovadas.

Integração RAG e sincronização em stack Supabase descartável (24/09/2026): a base partilhada tinha histórico de migrations divergente, por isso criei uma stack temporária com project ID/portas próprias, sem resetar nem aplicar SQL à base existente. As 26 migrations do branch aplicaram do zero; `db lint` em `public,private` sem erros; integração Supabase real 3/3 (Realtime, round-trip/conflito/tombstone/foto privada/equipa errada e RAG com isolamento de equipa); HTTP da Edge Function MCP/RAG 1/1, com provider ausente sem tráfego externo; E2E local sincronizado PC↔PWA 1/1. A stack temporária foi parada. `npm run check`, `npm test` (363 aprovados), Playwright completo 139/139 e `git diff --check` passaram. Sem publicação, configuração de secrets ou escrita no projeto remoto; RAG continua por ativar no ambiente partilhado até revisão.

Evidência de vídeo ligada a estatística (24/09/2026): acrescentei cobertura de interface para guardar um momento ligado a um golo contado e abrir o URL no segundo persistido, substituindo o timestamp anterior do vídeo. O teste confirma a referência estável `goals.for` e mantém a contagem de lances como origem da estatística. `npm run check`, `npm test` (363 aprovados, 0 falhas, 4 integrações locais ignoradas), Playwright completo 140/140 e `git diff --check` passaram. Sem alteração nos dados, nas imagens aprovadas ou na publicação.

Pesquisa histórica paginada (24/09/2026): apresenta 100 resultados por bloco e permite carregar mais sem repetir a pesquisa; mostra o total correspondente e mantém até 500 resultados leves. Acima desse limite explica que os mais antigos ficam fora da lista e recomenda filtrar por data. E2E percorre 240 resultados até aos mais antigos, verifica o teto de 500/501 e viewport móvel 390 px e desktop 1440 px sem overflow; pesquisa focada 6/6. O Playwright completo imediatamente antes desta verificação passou 144/144; `npm run check`, `npm test` (365 aprovados, 4 integrações locais ignoradas) e `git diff --check` passaram. Apenas local, sem publicação ou alteração de dados/imagens.
