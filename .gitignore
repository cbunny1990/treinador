# Backend FastAPI antigo, mantido localmente para referência mas não publicado
# (o repo passou a ser a PWA offline na raiz)
app/
*.db
server.log
__pycache__/
*.pyc

# Dados reais de equipas/jogadores: ficam apenas no dispositivo do treinador.
*.private.json
node_modules/
test-results/
playwright-report/
supabase/.temp/
